# Archon — Status Report

> این فایل خلاصه‌ی وضعیت پروژه‌ست. هر وقت گفتید «استاتوس رو ثبت کن»، این فایل رو با آخرین وضعیت آپدیت می‌کنم.
> طراحی شده که قابل کپی/انتقال به یک AI دیگه باشه برای مشاوره — همه چیز خودایستا و بدون نیاز به context قبلی نوشته می‌شه.

**آخرین آپدیت:** 2026-08-26 (دور سی‌وهفتم) — 💬 پنل جزئیات قرارداد/پروژه در صفحه Messages (الهام از آپورک) + غنی‌سازی جزئیات پروژه. Milad اسکرین‌شات‌هایی از پنل مسیج و جزئیات جاب آپورک فرستاد و خواست ستون سمت راست Messages هم مشابه اون، جزئیات قرارداد/مایلستون/پرداخت/کارفرما رو نشون بده؛ همچنین خواست جزئیات پروژه بررسی و موارد مفید بهش اضافه بشه.
- **کامپوننت جدید `ThreadDetailsPanel`** — ستون سوم سمت راست Messages (فقط دسکتاپ): وقتی گفتگو به یک قرارداد وصله، خلاصه‌ی پروژه (عنوان/توضیح/ددلاین/دسته)، نوار پیشرفت پرداخت (مبلغ آزادشده از کل مبلغ)، و لیست کامل مایلستون‌ها (عنوان، مبلغ، وضعیت، ددلاین) رو نشون می‌ده — همه از همون endpoint موجود `GET /marketplace/contracts/{id}` (بدون نیاز به endpoint جدید). وقتی گفتگو مستقیم (بدون قرارداد) باشه، به‌جاش یک کارت «درباره‌ی این فرد» (بیو، لوکیشن، مهارت‌ها، امتیاز) از `GET /marketplace/members/{id}` نشون می‌ده.
- بک‌اند `serialize_contract` گسترش پیدا کرد: توضیح/ددلاین/دسته‌ی پروژه + آواتار و «عضو از تاریخ» هر دو طرف قرارداد اضافه شد — بدون نیاز به endpoint جدید.
- **بخش «Skills needed» و «Experience level» به پروژه‌ها اضافه شد** (ستون‌های جدید `skills`/`experience_level` روی جدول پروژه، auto-migrate). قابل تعیین موقع ثبت پروژه، قابل ویرایش بعداً، و به‌صورت چیپ/بج روی کارت‌های لیست و صفحه‌ی جزئیات نمایش داده می‌شه.
- **نوار «Activity» جدید** روی صفحه‌ی جزئیات پروژه: تعداد پروپوزال، تعداد کل پروژه‌های پست‌شده توسط همون کارفرما، چند روز پیش پست شده؛ ددلاین هم یک شمارش معکوس روز و رنگ هشدار (وقتی ≤۳ روز مونده) گرفت.
تست شد: تایپ‌چک تمیز؛ بک‌اند — دو اکانت تستی واقعی ساخته شد، یک پروژه با مهارت/سطح تجربه پست شد، پروپوزال ثبت و تایید شد (قرارداد واقعی ساخته شد)، `GET /marketplace/contracts/{id}` مقادیر جدید رو درست برگردوند؛ مرورگر — پنل Messages با دیتای واقعی (کارفرما/فریلنسر واقعی، مایلستون واقعی، نوار پرداخت) رندر شد، صفحه‌ی جزئیات پروژه چیپ‌های مهارت/بج سطح تجربه/نوار Activity/شمارش معکوس ددلاین همه با دیتای واقعی درست نمایش داده شدن، فرم ثبت پروژه فیلدهای جدید رو داشت، بدون خطای کنسول واقعی. پیش از اون (دور سی‌وششم) — 🏗️ تکمیل بازطراحی مارکت‌پلیس + استانداردسازی حالت‌های خالی/لودینگ + پاس موبایل + حذف صفحه‌ی مرده + آیکون واقعی برای پیام‌های وضعیت. Milad یک لیست ۵موردی داد و خواست همه با دسترسی کامل انجام بشه و در آخر یک گزارش خلاصه بده.
- **۳ کامپوننت مشترک جدید** ساخته شد: `EmptyState` (آیکون+عنوان+توضیح+اکشن اختیاری)، `LoadingState` (اسپینر یکدست، حالت تمام‌صفحه یا این‌لاین)، `InlineStatus` (پیام‌های وضعیت `✓ Saved`/`✗ Failed` رو پارس می‌کنه و به‌جای متن رنگی خام، آیکون واقعی `CheckCircle2`/`XCircle`/`Info` + رنگ semantic نمایش می‌ده — بدون نیاز به تغییر شکل هیچ state ای، فقط تغییر رندر).
- **Projects و Contracts بازطراحی ساختاری شدند** (نه فقط رنگ): نوار هدر چسبان استاندارد (مثل بقیه‌ی صفحات)، `STATUS_META` توکنایز شده، `EmptyState`/`LoadingState` جایگزین متن خام لودینگ/خالی.
- **Messages**: بج‌های خوانده‌نشده و هایلایت گفتگوی انتخاب‌شده توکنایز شدن؛ `EmptyState`/`LoadingState` جایگزین حالت‌های دستی قبلی.
- **صفحات جزئیات پروژه/قرارداد** (`projects/[id]`, `contracts/[id]`) پالیش شدن: لودینگ/۴۰۴/خالی با کامپوننت‌های مشترک، رنگ‌های وضعیت/مایلستون توکنایز (به‌جز `released`ی بنفش که معادل توکن نداره و آگاهانه دست‌نخورده موند).
- **`InlineStatus` روی ۷ صفحه‌ی ادمین/مارکت‌پلیس** (`admin`, `admin/settings`, `admin/system-health`, `discovery`, `marketplace-admin`, `payments`, `verification`) جایگزین ۱۲ نمونه از الگوی دستی `msg.startsWith('✓')` شد.
- **صفحه‌ی مستقل `/users` کامل حذف شد** — دیگه هیچ‌جا لینک نداشت، کاملاً جایگزین‌شده با `AdminUsersPanel` داخل پنل ادمین.
- **پاس موبایل کامل روی هر ۳۳ صفحه انجام شد** (فقط بررسی، بدون تغییر) — پلتفرم از قبل کاملاً با الگوی `useIsMobile()` پوشش داده شده بود، هیچ صفحه‌ای نیاز به اصلاح نداشت.
تست شد: `npx tsc --noEmit` روی کل ریپو تمیز (بعد از پاک‌کردن کش `.next` برای رفع خطای مسیر مرده‌ی `/users`)؛ مرورگر (لاگین واقعی) — Projects/Contracts/Messages هرکدوم با EmptyState/LoadingState جدید درست رندر شدن، بدون خطای کنسول واقعی. پیش از اون (دور سی‌وپنجم) — 🎨 رفع رنگارنگی صفحه‌ی Tasks. Milad گفت این صفحه هم هنوز رنگی‌رنگیه.
- **باگ پیدا شد**: `TASK_COLORS` به هر نوع تسک AI (email/review/followup/research) و تسک شخصی یک رنگ تزئینی جدا (آبی/بنفش/کهربایی/سبز/صورتی) برای border+background می‌داد، بدون معنای وضعیتی — دقیقاً همون الگویی که این دور توی همه‌جا رفع شده. حالا همه‌ی کارت‌های تسک یک استایل خنثی مشترک (`var(--bg-card)`/`var(--border)`) دارن؛ پنل «Add Task» که صورتی بود به `var(--accent-dim)` تبدیل شد (هماهنگ با بقیه‌ی فرم‌های اپ). آیکون هر نوع تسک (Mail/Eye/RefreshCw/...) دست‌نخورده موند چون یک شناسه‌ی کاربردیه، نه تزئین.
تست شد: تایپ‌چک تمیز؛ مرورگر — یک تسک شخصی تستی اضافه شد و تایید شد کارتش دقیقاً همون رنگ خنثی بقیه‌ی کارت‌های اپ رو داره، مودال حذف با رنگ قرمز semantic درست کار کرد، بدون خطای کنسول. پیش از اون (دور سی‌وچهارم) — 🐛 رفع باگ Heat Distribution + اصلاحات چیدمان Analytics طبق بازخورد. Milad گفت اعداد Heat Distribution با چیزی که «Recalculate Heat» توی پنل ادمین نشون می‌ده فرق داره.
- **باگ پیدا شد**: Heat Distribution مستقیم از ستون ذخیره‌شده‌ی `UserCompanyState.heat_level` می‌خوند، ولی این ستون فقط با دکمه‌ی Recalculate Heat آپدیت می‌شه و حتی اونجا هم فقط وقتی «تغییر» تشخیص داده بشه بازنویسی می‌شه — یعنی heat واقعی یک شرکت می‌تونست live عوض بشه (مثلاً طبق قانون داخلی `heat_for()` بعد از ۳۰ روز بدون فعالیت سرد بشه) ولی عدد ذخیره‌شده هنوز مقدار قدیمی رو نشون بده. رفع شد: حالا دقیقاً با همون تابع `heat_for()` که `company_to_dict` در همه‌جای دیگه‌ی اپ استفاده می‌کنه، live محاسبه می‌شه — تست شد و الان دقیقاً با خروجی خود endpoint دکمه‌ی Recalculate یکی می‌شه.
- **Quick Insights منتقل شد** به خط دوم بخش Overview، درست زیر کارت‌های KPI (قبلاً ته بخش My Activity بود).
- **بخش Lead Hunter ROI حذف شد** (endpoint بک‌اندش هم چون دیگه هیچ‌جا استفاده نمی‌شد پاک شد).
- **کارت Pipeline Funnel حذف شد** — بخش Pipeline فقط شامل آمار فشرده‌ی Pipeline Velocity موند.
تست شد: بک‌اند — `heat_counts` قبل/بعد از صدازدن `POST /recalculate-heat` دقیقاً با پاسخ خود آن endpoint یکی بود؛ مرورگر — چیدمان جدید تایید شد، بدون خطای کنسول. پیش از اون (دور سی‌وسوم) — 📊 بازطراحی عمیق Analytics: ساختار بخش‌بندی‌شده + ۶ متریک تازه از داده‌ی از‌قبل‌موجود. Milad خواست Analytics هم مثل پروفایل و پنل ادمین یک بازطراحی ساختاری واقعی بگیره — نه فقط رفع رنگ، بلکه چیدمان بهتر + ایده‌های خلاقانه برای متریک‌های جاافتاده + نمودارهای کاربردی با داده‌ی دقیق.
- **بررسی بک‌اند** نشون داد چندتا منبع داده‌ی آماده و ایندکس‌شده هست که هیچ‌جا استفاده نمی‌شد: `UserCompanyState.heat_level`، `Company.opportunity_score`، `EmailReputationEvent` (رویدادهای واقعی sent/replied)، `DiscoveryHunt` (فیلدهای از‌قبل‌تجمیع‌شده)، `DailyTask.is_done`، `Campaign.tone`.
- **صفحه به ۶ بخش با هدر واضح تقسیم شد**: Overview، Reply Trend، Pipeline، Lead Generation، My Activity، Catalog — با یک ناوبری «پرش به بخش» چسبان سمت چپ (الگوی دو‌ستونه‌ی sticky-aside پروفایل/ادمین، این‌بار برای پیمایش داخل یک صفحه‌ی طولانی).
- **۶ متریک تازه اضافه شد**، همه بدون تغییر schema: **Heat Distribution** (hot/warm/cold)، **Score Distribution** (هیستوگرام ۴بازه‌ای)، **Reply Rate Trend** (نمودار واقعی روزانه/هفتگی/ماهانه با هاور تولتیپ از رویدادهای واقعی reputation — نه یک پروکسی از Campaign مثل نمودار قدیمی «۶ ماه اخیر» که حذف شد)، **Pipeline Velocity** (میانگین روز ماندن در هر مرحله)، **Lead Hunter ROI** (نرخ تبدیل found→added هر Hunt)، **Task Completion Rate** (۳۰ روز اخیر) و **Reply Rate by Tone** (کدوم لحن ایمیل بهترین جواب رو می‌گیره).
- بک‌اند: `GET /companies/analytics/summary` گسترش پیدا کرد (فیلدهای قدیمی دست‌نخورده)، دو endpoint جدید (`reply-trend` با پارامتر interval، `discovery-roi`) — الگوی bucket‌بندی زمانی دقیقاً کپی همون تابع `admin_growth` (پایتونی، نه cast دیتابیسی که قبلاً کرش کرده بود). کامپوننت جدید `ReplyTrendChart.tsx` (کپی الگوی `GrowthChart`). رنگ‌ها فقط `var(--accent)` + semantic واقعی — هیچ رنگ تزئینی جدید، هم‌راستا با پاکسازی رنگی این دور.
تست شد: تایپ‌چک تمیز؛ بک‌اند — هر ۳ endpoint با دیتای واقعی درست جواب دادن (`pipeline_velocity_days: 11.1`, `heat_counts`, `score_buckets` همه محاسبه‌ی درست)؛ مرورگر — هر ۶ بخش با ناوبری پرش رندر شدن، Reply Trend Chart تاگل بازه کار کرد، موبایل بدون overflow افقی، بدون خطای کنسول. پیش از اون (دور سی‌ودوم) — 🎨 بررسی کامل کل پلتفرم برای رنگارنگی باقی‌مونده. Milad خواست بقیه‌ی صفحاتی که هنوز رنگی موندن هم بررسی بشه — یک ممیزی کامل روی همه‌ی ~۳۰ صفحه‌ی `page.tsx` انجام شد.
- **دوتا صفحه‌ی دیگه پیدا شد**: `/admin/revenue` (۴ کارت KPI بالای صفحه — All-time/MRR/This month/This week — هرکدوم آبی/بنفش/سبز/کهربایی بدون معنای واقعی) و `/map` (۳ تایل آمار هدر — Countries/Companies/Hot Leads — و تایل «Hot Leads» توی پنل جزئیات کشور، هر دو رنگ تزئینی داشتن). هر دو به `var(--text)` خنثی تبدیل شدن.
- **بقیه‌ی صفحات تمیز بودن**: هر رنگ باقی‌مونده‌ای که پیدا شد یا `STATUS_META`/`STATUS_COLOR` (وضعیت پایپلاین: new/sent/replied/client/...، دقیقاً همون پالتی که Dashboard و Company Detail هم استفاده می‌کنن)، یا `PLAN_COLORS` (سطح پلن: trial/basic/pro/agency)، یا بازخورد وضعیت واقعی (موفق/خطا/هشدار)، یا legend چگالی تک‌رنگ نقشه (Low→High) بود — همه‌ی این‌ها معنای واقعی دارن، نه تزئین، پس دست‌نخورده موندن.
تست شد: تایپ‌چک تمیز؛ مرورگر — هر دو صفحه با دیتای واقعی رندر شدن، بدون خطای کنسول. پیش از اون (دور سی‌ویکم) — 🎨 رفع رنگارنگی صفحات Weekly Report و Analytics. Milad اسکرین‌شات فرستاد و گفت این دو صفحه هنوز رنگی‌رنگی‌ان و با زبان دیزاین سیستم هماهنگ نیستن — دقیقاً همون مشکلی که قبلاً توی پنل ادمین رفع شده بود (هر کارت/متریک یک رنگ تزئینی جدا، بدون معنای واقعی).
- **Weekly Report**: هر ۶ کارت بخش (Executive Summary، Pipeline Health، Top Leads، Follow-up، Email Performance، Goals) قبلاً هرکدوم رنگ خودشون رو داشتن (آبی/بنفش/کهربایی/نارنجی/سبز/فیروزه‌ای) — الان همه یک استایل خنثی مشترک (`var(--bg-card)`/`var(--border)`، آیکون با `var(--text-muted)`، هاور با `var(--accent)`) دارن. کارت انگیزشی پایین صفحه هم به `var(--accent-dim)` تغییر کرد. ایموجی پرچم روی دکمه‌های زبان هم حذف شد (برچسب متنی already کافیه).
- **Analytics**: کارت‌های KPI بالای صفحه، ویجت‌های «My Outreach»، نوار‌های پیشرفت «Email Campaign»، و نمودار میله‌ای «Industries» (که قبلاً رنگ‌هاشون چرخشی از یک پالت ۵تایی رنگین‌کمانی بودن) همه به `var(--accent)`/`var(--text)` یکدست شدن. رنگ‌های semantic واقعی (سبز/زرد برای کیفیت Reply Rate) نگه داشته شدن چون واقعاً وضعیت رو نشون می‌دن، نه تزئین. رنگ‌های Pipeline Funnel (هر وضعیت یک رنگ) عمداً دست‌نخورده موند چون معنای واقعی داره و دقیقاً همون پالت وضعیتیه که توی Dashboard/Company Detail هم استفاده می‌شه.
تست شد: تایپ‌چک تمیز؛ مرورگر — هر ۲ صفحه با دیتای واقعی رندر شدن، بدون خطای کنسول؛ با اسکریپت مستقیم تایید شد هر ۶ کارت گزارش هفتگی الان دقیقاً یک `background-color`/`border-color` مشترک دارن (قبلاً هرکدوم فرق می‌کرد). پیش از اون (دور سی‌ام) — 🎨 لندینگ‌پیج هم به دیزاین سیستم منتقل شد. Milad پرسید لندینگ چک شده یا نه — بررسی نشون داد `frontend/public/landing.html` یک فایل HTML استاتیک کاملاً جداست که مستقیم روی مسیر `/` سرو می‌شه (`next.config.ts` یک rewrite داره: `"/" → "/landing.html"`)، بیرون از درخت اپ Next.js — پس هیچ‌وقت توی رول‌اوت فاز ۷ (دور قبلی) پوشش داده نشده بود.
- پالت رنگی مستقل خودش داشت (رنگ برند قدیمی `#4F7BF7`/`#7C3AED`، فونت Space Grotesk+Inter) — الان متغیرهای CSS خودش (`--ink`, `--text`, `--muted`, `--border`, `--accent-a`, `--line`, `--violet`, `--ok`, `--amber`, ...) دقیقاً همون مقادیر روشن/تیره‌ی `app/globals.css` رو دارن، و فونت به Vazirmatn (+ JetBrains Mono برای اعداد) تغییر کرد.
- ایموجی‌های تابعی (🌙☀️ تاگل تم، 🌐 سوییچ زبان، 🇬🇧🇮🇷 پرچم گیت زبان، 🎨🏢 آیکون‌های بخش مارکت‌پلیس) با SVG دستی به سبک lucide جایگزین شدن — چون این فایل استاتیکه و باندلر نداره، نمی‌شه مستقیم از پکیج `lucide-react` استفاده کرد؛ پرچم‌ها هم به بج متنی EN/FA تبدیل شدن (هم‌راستا با قانون «بدون ایموجی به‌عنوان آیکون»).
تست شد: مرورگر — گیت زبان با بج‌های EN/FA درست نمایش داده شد، تاگل تم (روشن/تیره) بین دو آیکون SVG سوییچ کرد و کلاس `light-theme` درست اعمال شد، بدون خطای کنسول واقعی (فقط چندتا درخواست باقیمونده از تب قبلی به بک‌اند خاموش، بی‌ربط به لندینگ). پیش از اون (دور بیست‌ونهم) — 🎨 پایان کامل رول‌اوت دیزاین سیستم فاز ۷ در کل پلتفرم. Milad یک گزارش کامل خواست که کدوم صفحات به تم/تایپوگرافی/دیزاین سیستم جدید (توکن‌های `var(--bg-card)`/`var(--text)`/`var(--accent)`/... + آیکون‌های `lucide-react` به‌جای ایموجی خام) منتقل شدن و کدوم نه؛ بعد از دیدن گزارش، دستور داد همه‌ی موارد باقی‌مونده بدون نیاز به تایید بیشتر انجام بشه.
- **بلاک‌کننده‌های سراسری اول** (چون روی تقریباً همه‌ی صفحه‌ها اثر می‌ذارن): `NotificationBell` (بدترین وضعیت — ۱۳ ایموجی، تقریباً بدون توکن)، `Sidebar` (آخرین ایموجی باقی‌مونده‌ش — چیپ وضعیت احراز هویت)، `OnboardingTour` (تجربه‌ی اولین ورود کاربر جدید)، `AccessLock`، `ContractChat`، `VerifiedBadge`، `MarketplaceBeta` — همه به lucide + توکن منتقل شدن.
- **کل مسیر auth از صفر منتقل شد**: `/login`، `/signup`، `/forgot-password`، `/reset-password` قبلاً هیچ `var(--...)`ی نداشتن (رنگ‌های خام هاردکد، مستقل از سیستم توکن) — با اینکه به‌طور تصادفی رنگ کبالت تم لایت رو تقلید می‌کردن، ساختاری به توکن‌ها وصل نبودن. الان کامل روی سیستم توکن‌ان (چشم رمز عبور، آیکون‌های موفقیت با lucide).
- **`/import` بازنویسی کامل شد**: قبلاً کاملاً روی کلاس‌های Tailwind با پالت روشن ثابت بود، جدا از بقیه‌ی اپ — الان با `Sidebar` + توکن‌ها + آیکون‌های lucide، دقیقاً هم‌الگو با `/add`.
- **باقی‌مونده‌ی ~۲۵ صفحه (که توکن رنگ داشتن ولی هنوز ایموجی به‌جای آیکون استفاده می‌کردن) با ۵ ایجنت موازی migrate شدن**: dashboard، company detail، admin overview، profile (+ security + پروفایل عمومی + پیش‌نمایش عضو)، discovery، marketplace-admin، users، verification، waitlist، payments، tasks، upgrade، map، analytics، feed، report، projects (+ جزئیات)، contracts (+ جزئیات)، messages، edit — به‌همراه پاکسازی دستی چندتا کامپوننت مشترک باقی‌مونده (`AdminUsersPanel`, `MemberPreviewDrawer`, `PublishSection`).
- **محدودیت آگاهانه‌ی scope**: پیام‌های وضعیت رشته‌ای با پیشوند ✓/✗ (مثل `setMsg('✓ Saved')` / `msg.startsWith('✓')`) — یک الگوی رایج در کل کدبیس — عمداً دست‌نخورده موند، چون تبدیلش به آیکون واقعی نیاز به بازسازی شکل state (از یک رشته‌ی ساده به `{ok, text}`) توی ده‌ها نقطه داره؛ این یک ریفکتور state-shape جداست، نه یک تغییر ظاهری ساده. همچنین ایموجی‌های داخل `&lt;option&gt;` تگ‌های `&lt;select&gt;` خام (مثل انتخابگر Heat Level) دست‌نخورده موندن چون `&lt;option&gt;` فقط متن ساده می‌پذیره و نمی‌تونه آیکون SVG رندر کنه.
تست شد: `npx tsc --noEmit` روی کل ریپو بعد از هر مرحله (نهایتاً کاملاً تمیز)؛ grep نهایی برای ایموجی‌های تابعی تایید کرد فقط دو دسته باقی موندن (پیام‌های وضعیت رشته‌ای، و گزینه‌های `&lt;option&gt;`) — هردو آگاهانه خارج از scope. مرورگر: `/dashboard`، جزئیات شرکت، `/profile`، `/login` (بعد از خروج از حساب)، `/import` همه با آیکون‌های lucide و بدون خطای کنسول تایید شدن. پیش از اون (دور بیست‌وهشتم) — 🐛🚀 رفع باگ واقعی حذف کاربر + نقش Founder + نمودار رشد دقیق‌تر + نرخ ارز دستی/خودکار + لاگ عیب‌یابی کلی پلتفرم.
- **باگ حذف کاربر پیدا و رفع شد**: `_purge_user_data` فقط ۸ تا از ~۲۰ جدولی که به `users.id` وابسته‌ان رو پاک می‌کرد (نوتیفیکیشن، پست‌ها، احراز هویت، مکالمات، ریویوها، Discovery Hunts و... جا افتاده بودن) — هر کاربر واقعی با هرکدوم از این‌ها، حذفش با خطای دیتابیس (Foreign Key) شکست می‌خورد. حالا همه‌ی جدول‌های وابسته قبل از حذف پاک می‌شن. حین تست، خود **لاگ عیب‌یابی جدید** (پایین همین آپدیت) بلافاصله یک باگ دوم رو لو داد: ستون کاربر توی جدول `PostReport` اسمش `reporter_id`ه نه `user_id` — با کد قدیمی تایپ‌شده اشتباه، الان درست شد. تست شد: حذف ۴ کاربر تستی واقعی (با دیتای متنوع) بدون خطا انجام شد.
- **نقش Founder اضافه شد**: اکانت `milad.rostami9999@gmail.com` به‌عنوان Founder علامت‌گذاری می‌شه (`is_founder`، مشتق از ایمیل — نقش واقعی دیتابیسی (`role`) همچنان `admin` می‌مونه تا هیچ‌کدوم از چک‌های دسترسی موجود توی کد نشکنه). `PATCH /auth/users/{id}` حالا هر تلاش برای تغییر این اکانت رو با خطای ۴۰۳ رد می‌کنه — نه ادمین دیگه‌ای، نه هیچکس. توی پنل کاربران، این اکانت با بج «🛡 Founder» و برچسب «🔒 Locked» (به‌جای Edit/Disable/Delete) نمایش داده می‌شه، و همیشه (به‌همراه بقیه‌ی ادمین‌ها) اول لیست پین می‌شه، فارغ از سورت انتخاب‌شده.
- **سورت تب Users**: یک دراپ‌داون «Most recent» / «By plan (Agency → Basic)» اضافه شد؛ Founder و ادمین‌ها همیشه اول لیست می‌مونن.
- **نمودار رشد (GrowthChart)**: حالا ۳ حالت داره — روزانه (۳۱ روز اخیر)، هفتگی (۱۲ هفته)، ماهانه (۱۲ ماه) — بک‌اند این باکت‌بندی رو توی پایتون انجام می‌ده (نه `cast(DateTime,Date)` سمت دیتابیس، که قبلاً یک‌بار روی SQLite کرش کرده بود) تا روی SQLite و Postgres یکسان کار کنه. با هاور روی هر ستون، یک تولتیپ عدد دقیق رو نشون می‌ده، و پایین نمودار همیشه بازه‌ی زمانی (یا برچسب ستون هاورشده) رو نشون می‌ده.
- **نرخ ارز — حالت خودکار/دستی**: `services/exchange.py` حالا یک `AppSetting` جدید (`exchange_rate_mode`) داره. توی حالت **Auto** (پیش‌فرض)، منبع اول همیشه tgju.org است (با fallback به منبع دوم و کش نهایی)؛ صفحه‌ی System Health همیشه با `force=True` تازه‌ترین عدد رو می‌گیره (نه کش ۱ساعته‌ی بقیه‌ی صفحات). توی حالت **Manual**، یک عدد ثابتی که ادمین وارد کرده همه‌جا استفاده می‌شه و دیگه هیچ فراخوانی شبکه‌ای انجام نمی‌شه. دکمه‌ی «Edit» جدید توی کارت Exchange Rate این دو حالت رو سوییچ می‌کنه.
- **لاگ عیب‌یابی کلی پلتفرم (جدید)**: جدول `PlatformLog` + یک exception handler سراسری توی `main.py` که هر خطای ۵۰۰ مدیریت‌نشده (نه خطاهای عمدی مثل ۴۰۳/۴۰۴) رو با منبع (مسیر endpoint)، پیام، traceback کامل، و ساعت/تاریخ دقیق ثبت می‌کنه — **فقط رویدادهای کلی/سیستمی، نه فعالیت شخصی کاربران** (که جدا و از قبل توی History/AdminActivityLog هست). یک جاب کرون جدید شبانه (`platform_log_cleanup`, هر روز ۰۲:۰۰ UTC) ردیف‌های قدیمی‌تر از ۱۴ روز رو خودکار پاک می‌کنه. بخش جدید «Platform diagnostics» توی System Health لیست رو نشون می‌ده (فیلتر بر اساس سطح خطا/هشدار/اطلاع، کلیک برای باز کردن traceback کامل) + دکمه‌ی Clear دستی. **همین سیستم بود که باگ `PostReport.reporter_id` بالا رو در عمل پیدا کرد** — دقیقاً همون چیزی که Milad خواسته بود: «منشا و دلیل عیب رو مشاهده کنیم».
تست شد end-to-end: بک‌اند — حذف ۴ کاربر واقعی موفق؛ `GET/PUT /auth/admin/exchange-rate/settings` سوییچ auto↔manual رو درست ذخیره و apply کرد؛ `GET /auth/admin/growth?interval=day|week|month` هرسه حالت رو با bucket‌بندی درست برگردوند؛ `is_founder` روی اکانت درست فقط true بود. مرورگر — Users tab: Founder با بج و قفل درست بالای لیست (قبل از ادمین معمولی)، دراپ‌داون سورت کار کرد؛ System Health: تاگل Daily/Weekly/Monthly نمودار رشد + هاور تولتیپ کار کرد، Edit نرخ ارز بین Auto/Manual سوییچ کرد و مقدار درست ذخیره/نمایش داده شد، بخش Platform Diagnostics دقیقاً همون خطای واقعی `PostReport` رو قبل از رفعش نشون داد؛ بدون خطای کنسول. پیش از اون (دور بیست‌وهفتم) — 🔧 اصلاحات پنل ادمین بعد از تست Command Center: Milad گفت رفتن بخش Users به یک صفحه‌ی جدا (`/users`) با تجربه‌ی تک‌صفحه‌ای بقیه‌ی پنل ادمین (که همه چیز با تعویض تب انجام می‌شه) ناهماهنگه.
- **مدیریت کاربران حالا داخل خود `/admin` نمایش داده می‌شه** — یک کامپوننت جدید `AdminUsersPanel.tsx` که کل منطق/UI صفحه‌ی قدیمی `/users` رو (لیست، فیلتر، ادیت/غیرفعال‌سازی/حذف، Add User دستی، مودال جزئیات) بدون تغییر منطق در خودش داره، و توی `/admin/page.tsx` با یک state ساده (`view: 'overview' | 'users'`) به‌جای Overview نمایش داده می‌شه — بدون هیچ ناوبری/navigation واقعی، دقیقاً یک تعویض تب.
- **لینک «Users» در `AdminSideNav`** حالا (فقط وقتی داخل `/admin`ه) یک دکمه‌ست که این state رو toggle می‌کنه، نه یک `<a href>`. از بقیه‌ی صفحات پنل ادمین (Revenue/System Health/Settings) یا از پالت جستجو، کلیک روی Users به `/admin?tab=users` می‌ره که `/admin` موقع mount این query param رو می‌خونه و مستقیم تب Users رو باز می‌کنه.
- **لینک «Users» از سایدبار اصلی حذف شد** (طبق درخواست صریح) — چون دیگه صفحه‌ی مجزایی نیست که نیاز به لینک جدا داشته باشه؛ دسترسی فقط از داخل Admin Panel.
- **کارت «Export Users» به تب Overview اضافه شد** — از endpoint موجود `GET /auth/users/export` استفاده می‌کنه (که از قبل CSV با BOM تولید می‌کرد، مستقیم توی اکسل باز می‌شه — نیازی به کتابخونه یا endpoint جدید نبود).
- **افزودن کاربر دستی** از قبل توی همون منطق منتقل‌شده وجود داشت (فرم Add User با نام/ایمیل/پسورد/پلن، از `POST /auth/users` موجود) — فقط جاش عوض شد، قابلیتی جدید بهش اضافه نشد چون از قبل کامل بود.
تست شد: تایپ‌چک تمیز؛ مرورگر — کلیک روی Users توی ناوبری چپ صفحه رو عوض نکرد (فقط محتوا سوییچ شد، بدون navigation)، لیست ۱۶ کاربر واقعی با همه‌ی اکشن‌ها (View/Edit/Message/Disable/حذف) رندر شد، کلیک روی Overview برگشت به حالت اول، کارت Export Users توی گرید Tools ظاهر شد، سایدبار اصلی دیگه لینک Users نداره، بدون خطای کنسول. پیش از اون (دور بیست‌وششم) — 🚀 پنل ادمین به «Command Center» تبدیل شد: لاگ فعالیت، سلامت سیستم، نمودار رشد، کاربران در معرض ریسک، Feature Flags، ناوبری چپ مشترک + جستجوی سراسری (Ctrl/Cmd+K). Milad از ۶ ایده‌ی پیشنهادی دور قبل، ۵ تاش (بدون شماره ۴ که با ایده‌ی جستجوی سراسری پوشش داده شد) و هر ۳ ایده‌ی ساختاری رو تایید کرد.
- **پنل ادمین از یک صفحه به ۴ صفحه تبدیل شد**: `/admin` (Overview)، `/admin/revenue` (موجود، فقط ناوبری اضافه شد)، `/admin/system-health` (جدید)، `/admin/settings` (جدید) — همه پشت یک کامپوننت مشترک `AdminSideNav` (ستون چپ sticky، دقیقاً الگوی پروفایل) و `AdminCommandPalette` (مودال وسط‌صفحه، اولین مودال centered این کدبیس، نه drawer).
- **لاگ فعالیت ادمین (Audit Trail)**: جدول جدید `AdminActivityLog` + سرویس `services/audit.py` (`log_admin_action`, دقیقاً الگوی `notify()` موجود — هیچ‌وقت raise نمی‌کنه). به ۸ نقطه‌ی موجود وصل شد: تغییر Plan Limit، ادغام کشورها، حذف دسته‌جمعی شرکت‌ها، ارسال Broadcast، ویرایش تنظیمات پرداخت، تایید/رد پرداخت، تایید Waitlist، تغییر Feature Flag — هرکدوم فقط یک خط اضافه شد، منطق موجود دست‌نخورده.
- **سلامت سیستم**: جدول جدید `CronRunLog` — `digest_scheduler.py`'s `_run()` حالا بعد از هر اجرای جاب (موفق یا ناموفق) یک ردیف می‌نویسه (قبلاً فقط `print()` می‌کرد که با هر ری‌استارت پاک می‌شد). صفحه‌ی جدید `/admin/system-health` وضعیت ۵ جاب کرون (آخرین اجرا + اجرای بعدی)، آخرین بک‌آپ (از endpoint موجود، بدون endpoint جدید)، تازگی نرخ ارز، و لاگ فعالیت اخیر رو نشون می‌ده. Manual Backup از Overview به اینجا منتقل شد.
- **نمودار رشد**: کامپوننت `GrowthChart.tsx` (همون الگوی نمودار میله‌ای CSS خام صفحه‌ی Revenue، بدون کتابخونه‌ی جدید) با تاگل Users/Companies، از endpoint جدید `GET /auth/admin/growth?days=30` (بدون migration — `GROUP BY func.date(created_at)`).
- **کاربران در معرض ریسک**: ویجت توی Overview از `GET /auth/admin/at-risk-users` — سیگنال‌های از قبل موجود روی مدل: پرداخت `pending` تاییدنشده، `last_login` خالی/قدیمی‌تر از ۳۰ روز، `plan_expires_at` ظرف ۷ روز آینده، هرکدوم با برچسب دلیل.
- **Feature Flags**: رجیستری ثابت کوچیک (۳ فلگ نمونه) روی `AppSetting` موجود (namespace `feature_flag_*`) — عمداً **نه** یک سیستم پویا/ساخت فلگ دلخواه، چون over-engineering بود؛ توی UI صادقانه نوشته شده که این فقط زیرساخت ذخیره‌سازیه و فعلاً جای دیگه‌ای کد بهش گوش نمی‌ده. Drawer ششم جدید کنار ۵تای قبلی توی `/admin/settings`.
- **جستجوی سراسری**: عمداً محدود به ناوبری/اکشن‌های ادمین شد (نه جستجوی زنده‌ی دیتابیس کاربر/شرکت) چون هیچ الگوی Cmd+K یا سرچ زنده‌ای قبلاً کدبیس نبود — اگه جستجوی داده هم لازم شد، قدم جداگونه.
تست شد end-to-end: بک‌اند — چند اکشن تستی (تغییر Plan Limit) → تایید `AdminActivityLog` ردیف ساخت و `GET /admin/activity-log` برگردوند؛ `GET /admin/growth`, `/admin/at-risk-users`, `/admin/system-health`, `/admin/feature-flags` با دیتای واقعی seed‌شده تست شدن (یک باگ واقعی حین تست پیدا و رفع شد: `cast(DateTime, Date)` روی SQLite با درایور فعلی خطای `TypeError` می‌داد، با `func.date()` جایگزین شد). مرورگر: هر ۴ صفحه با ناوبری چپ یکسان، Ctrl+K پالت رو باز کرد و فیلتر زنده کار کرد (تایپ «health» + Enter مستقیم به System Health رفت)، نمودار رشد و ویجت ریسک توی Overview درست رندر شدن، System Health وضعیت جاب‌ها/بک‌آپ/نرخ‌ارز/لاگ فعالیت رو نشون داد، Settings هر ۶ drawer (۵ قدیمی + Feature Flags جدید) رو باز کرد و toggle یک فلگ واقعاً در دیتابیس ذخیره شد (تایید مستقیم با API). موبایل بدون overflow افقی. پیش از اون (دور بیست‌وپنجم) — 🎨 پرداخت رنگی پنل ادمین: بعد از اسکرین‌شات واقعی، Milad گفت رنگارنگ‌بودن (هر KPI/تول یک رنگ جدا: بنفش/صورتی/نارنجی/فیروزه‌ای...) رو دوست نداره. همه‌ی کارت‌های KPI و Tools به یک استایل خنثی و یکدست (توکن‌های `--bg-card`/`--border`/`--accent`) تبدیل شدن — فقط رنگ سازمانی (کبالت) برای دکمه‌ها و hover، بدون رنگ‌بندی تزئینی جدا برای هر آیتم. رنگ‌های semantic (سبز/قرمز) فقط برای پیام‌های وضعیت واقعی (موفق/ناموفق) نگه داشته شدن، نه دکوراسیون.
- **کارت‌های تکراری با سایدبار حذف شدن**: User Management، Analytics، Waitlist، Payments، Marketplace Admin، Lead Hunter — همه‌شون از قبل توی گروه Admin/Workspace سایدبار لینک داشتن؛ نیازی به کارت جدا توی پنل ادمین نبود (طبق درخواست صریح Milad). Grid از ۴ ستون به ۳ ستون کاهش پیدا کرد چون فقط ۶ ابزار واقعاً منحصربه‌فرد پنل ادمین (Import/Export CSV، Recalculate Scores/Heat، Manual Backup، API Docs) باقی موند.
تست شد: تایپ‌چک تمیز، مرورگر — پنل ادمین بدون کارت‌های تکراری و بدون رنگارنگی، دکمه‌ها با گرادیان کبالت یکدست.

📋 **تحلیل و ایده‌های پیشنهادی برای پنل ادمین (به Milad ارائه شد، هنوز پیاده‌سازی نشده):**
چیزهایی که جاشون خالیه: (۱) لاگ فعالیت ادمین‌ها (audit trail) — چه کسی چه تغییری داد و کی، (۲) نمای سلامت سیستم (وضعیت APScheduler jobs، آخرین اجرای موفق/ناموفق هرکدوم، آخرین بک‌آپ)، (۳) نمودار رشد کاربران/شرکت‌ها در طول زمان (نه فقط عدد لحظه‌ای)، (۴) جستجوی سراسری در پنل ادمین (کاربر/شرکت/تراکنش با یک سرچ)، (۵) لیست کاربران در معرض ریسک (plan_status pending طولانی‌مدت، یا کاربرانی که هیچ فعالیتی نداشتن)، (۶) کنترل ویژگی‌ها (feature flags) برای رول‌اوت تدریجی قابلیت‌های جدید بدون دیپلوی. ایده‌های ساختاری/ظاهری: تبدیل پنل به یک صفحه‌ی «Command Center» با ستون چپ ثابت برای ناوبری بین زیربخش‌ها (شبیه الگوی پروفایل جدید)، یک نوار جستجوی سراسری بالای صفحه، و نمودارهای روند (نه فقط عدد) برای KPIهای اصلی — مشابه صفحه‌ی Revenue تازه‌ساخته‌شده. پیش از اون (دور بیست‌وچهارم) — 🎨 فاز ۷ / مرحله‌ی ۱۲: **بازطراحی پنل ادمین + صفحه‌ی جدید مدیریت درآمد**. اولین قدم از نقشه‌راه اصلی فاز ۷. Milad خواست پنل ادمین حرفه‌ای‌تر و جمع‌وجورتر بشه (تنظیمات نادر بره پشت دکمه‌ی ادیت/منوی پاپ‌آپ) و یک بخش مدیریت درآمد با تحلیل خودکار هفتگی/ماهانه اضافه بشه.
- **درآمد به‌عنوان یک صفحه‌ی جدا (`/admin/revenue`)** طراحی شد (تصمیم خودم، چون ادغامش توی پنل ادمین دقیقاً همون شلوغی‌ای رو بازتولید می‌کرد که Milad ازش شاکی بود) — با یک کارت خلاصه‌ی جمع‌وجور (این هفته/این ماه/کل/MRR تقریبی) بالای پنل ادمین + لینک «مشاهده‌ی کامل».
- **بک‌اند کاملاً جدید**: جدول `RevenueSnapshot`، سرویس `services/revenue.py` (تشخیص درآمد = لحظه‌ی تایید `PaymentRequest` توسط ادمین — دقیقاً طبق کامنت موجود توی مدل، نه لحظه‌ی ثبت درخواست کاربر؛ نرمال‌سازی IRR↔USD با سرویس نرخ ارز موجود)، ۲ endpoint جدید (`/auth/admin/revenue/summary`, `/auth/admin/revenue/timeseries`)، و ۲ جاب کرون جدید (Snapshot هفتگی شنبه‌ی تهران، ماهانه اول هر ماه میلادی) — دقیقاً با همون الگوی `digest_scheduler.py` که برای Daily Tasks/Weekly Report استفاده شده بود.
- **صفحه‌ی Revenue**: ۴ کارت KPI، نمودار میله‌ای CSS ساده (بدون کتابخونه‌ی جدید، هماهنگ با کانوانسیون موجود پروژه) با تاگل هفتگی/ماهانه — میله‌ی بازه‌ی جاری (هنوز کامل‌نشده) با حاشیه‌ی نقطه‌چین از بازه‌های تمام‌شده متمایزه — و جدول breakdown بر اساس پلن.
- **پنل ادمین جمع‌وجور شد**: کامپوننت مشترک جدید `AdminSettingsDrawer` (دقیقاً الگوی drawer شرکت/کاربر که قبلاً ساخته بودیم) — ۵ بخش کم‌تکرار (Plan Limits، ادغام کشورها، حذف دسته‌جمعی، Broadcast، دستورالعمل پرداخت) از «همیشه نمایش» به یک ردیف فشرده با دکمه‌ی «Edit →» که drawer رو باز می‌کنه تبدیل شدن — منطق/state هرکدوم دست‌نخورده موند. بنر بزرگ Lead Hunter حذف شد (تبدیل به یک تایل مثل بقیه). پنل ادمین حالا واقعاً هاب شد — لینک به Waitlist/Payments/Marketplace Admin (با نمایش تعداد pending) اضافه شد، چون قبلاً فقط به Users لینک می‌داد.
تست شد end-to-end: seed دستی چند `PaymentRequest` approved با پلن/ارز/تاریخ مختلف → تایید محاسبه‌ی صحیح `summary` (all-time/this-week/this-month/MRR) → صدازدن مستقیم `snapshot_period` و تایید ذخیره‌ی صحیح بازه‌ی هفته‌ی *قبلی* (نه جاری) → مرورگر: کارت درآمد و نمودار و breakdown درست رندر شدن، drawerها با Escape/کلیک باز و بسته شدن، موبایل بدون overflow. پیش از اون (دور بیست‌وسوم) — 🎨 پروفایل عمومی هم‌ساختار پروفایل اصلی شد: ستون راست حالا ۲ تب داره — «Profile» (شامل Description، Skills & Expertise، Portfolio، Education، Work Experience، همه در یک صفحه‌ی پیوسته، دقیقاً مثل تب ادغام‌شده‌ی پروفایل خصوصی ولی فقط‌خواندنی بدون دکمه‌ی ادیت) و «Posts» (دست‌نخورده). ستون چپ ساده‌تر شد (فقط هویت + تماس، چون تحصیلات/تجربه/مهارت‌ها دیگه توی تب Profile کامل نمایش داده می‌شن، دیگه تکراری نمی‌مونن). یک باگ واقعی حین تست موبایل/تبلت پیدا و رفع شد: یک قانون CSS سراسری قدیمی (`aside { width: 200px !important }` در یک media query تبلت، که برای Sidebar اصلی سایت نوشته شده بود) روی برچسب `<aside>` این صفحه هم اعمال می‌شد چون هردو از همون تگ HTML استفاده می‌کردن — ستون چپ پروفایل عمومی رو در حالت تبلت (۷۶۸–۱۰۲۳px) به ۲۰۰px به‌جای ۲۸۰px فشار می‌داد. رفع شد با تغییر تگ به `<div>` معمولی (بدون تغییر در قانون CSS سراسری، چون اون برای Sidebar واقعی هنوز لازمه). تست کامل ریسپانسیو انجام شد: دسکتاپ (۱۲۸۰px)، تبلت (۷۶۸px، بعد از رفع باگ)، موبایل (۳۷۵px) — بدون overflow افقی، تب‌ها full-width روی موبایل، دکمه‌ی سوییچ تم بدون همپوشانی با هدر، مودال پورتفولیو و لایت‌باکس درست کار کردن، بدون خطای کنسول. پیش از اون (دور بیست‌ودوم) — 🎨 پروفایل عمومی (`/u/[username]`) حالا از تم لایت هم پشتیبانی می‌کنه: قبلاً کل صفحه با رنگ‌های خام تیره (hex/rgba ثابت) نوشته شده بود، مستقل از سیستم توکن اصلی سایت، و هیچ دکمه‌ی سوییچ تمی نداشت. تمام رنگ‌ها به توکن‌های `var(--bg-main)`/`var(--bg-card)`/`var(--text)`/`var(--text-muted)`/`var(--text-dim)`/`var(--border)`/`var(--bg-input)` تبدیل شدن (همون‌هایی که بقیه‌ی سایت استفاده می‌کنه)، `PortfolioGrid` از `variant="dark"` به `variant="themed"` عوض شد. چون این صفحه (لینک اشتراک‌گذاری عمومی، بدون لاگین) هیچ Sidebar‌ای نداره که سوییچ تم رو مدیریت کنه، یک دکمه‌ی کوچیک شناور (☀️/🌙) بالا-راست صفحه اضافه شد که دقیقاً همون مکانیزم global رو استفاده می‌کنه (کلاس `light-theme` روی `<html>` + `localStorage['archon-theme']`, همون کلید و منطقی که `Sidebar.tsx` جای دیگه‌ی سایت استفاده می‌کنه) — یعنی اگه کاربر قبلاً از هرجای دیگه‌ی سایت تم لایت رو انتخاب کرده باشه، همون ترجیح این‌جا هم اعمال می‌شه. تست شد: سوییچ زنده‌ی رنگ پس‌زمینه/کارت‌ها/متن در کل صفحه، و ماندگاری انتخاب بعد از رفرش. پیش از اون (دور بیست‌ویکم) — 🎨 جای Description توی پروفایل عمومی اصلاح شد: بعد از اسکرین‌شات واقعی، Description (بیوی سابق) زیر تصویر توی ستون چپ خیلی گم بود. منتقل شد به یک کارت مجزا توی **ستون راست**، درست قبل از تب‌های Portfolio/Posts — دقیقاً همون‌جایی که Milad خواست. محدودیت کاراکتر بیو هم از ۶۰۰ به **۸۰۰** افزایش پیدا کرد (توی فرم ادیت تب Profile). پیش از اون (دور بیستم) — 🎨 آخرین پرداخت‌های ظاهری پروفایل بعد از اسکرین‌شات واقعی Milad:
- **بیو از ستون چپ کاملاً حذف شد** — چون کنار headline باعث شلوغی می‌شد؛ فقط توی ستون راست (تب Profile) می‌مونه.
- **«Bio» به «Description» تغییر نام داد** (فقط UI/لیبل‌ها — فیلد بک‌اند `bio` دست‌نخورده موند، بدون نیاز به migration).
- **Identity Verification خنثی شد**: رنگ سبز/برچسب «Verified» حذف شد (چون تیک آبی کنار اسم همین پیام رو می‌ده)، حالا فقط متن ساده با رنگ خنثی مثل Security settings. کد مرده‌ی مرتبط (`VERIFICATION_META`, state `verification`, fetch اضافه‌ی `/marketplace/verification/me`) هم پاک‌سازی شد چون دیگه لازم نبود.
- **آیکون‌های بخش Settings یکدست شدن**: هردو (Verification + Security) حالا آیکون لوسید خنثی دارن (`ShieldCheck`/`Lock`)، نه ایموجی رنگی.
- **Marketplace Mode منتقل شد به انتهای ستون چپ** به‌شکل یک اسلایدر دوحالته‌ی جمع‌وجور (Freelancer/Client)، به‌جای کارت بزرگ دوتایی که قبلاً بالای تب اصلی بود.
- **جواب به سوال Milad («این فقط نمادینه یا واقعاً اثر داره؟»)**: بررسی کد نشون داد **دروازه‌ی دسترسی نیست** — فقط تعیین می‌کنه کدوم تب پیش‌فرض توی `/projects` باز بشه (`client` → تب «My projects»، `freelancer` → تب باز) و یک برچسب («hires»/«takes work») کنار اسم کاربر جاهای دیگه نشون میده. هیچ endpoint بک‌اندی بر اساسش چیزی قفل/باز نمی‌کنه — هر دو حالت می‌تونن هم پروژه ثبت کنن هم پروپوزال بفرستن. نوشتن قوانین/role واقعی براش (اگه لازم باشه) یک تصمیم محصولی جداست که این دور بهش دست نزدیم.
تست شد: toggle اسلایدر واقعاً `PATCH /auth/me/account-mode` رو صدا زد و مقدار سرور تایید شد. پیش از اون (دور نوزدهم) — 🐛 رفع ۲ باگ واقعی در Daily Tasks + برگردوندن پنل Dashboard + جابه‌جایی Verification در پروفایل:
- **باگ ۱ — «همه‌ی کاربران یک لیست تسک یکسون می‌بینن»**: بررسی عمیق نشون داد این یک باگ isolation/امنیتی **نبود** — فیلتر `user_id` توی همه‌ی endpointها و جاب کرون کاملاً درست بود. ریشه‌ی واقعی: `backend/app/services/claude.py` پرامپت رو با جمله‌ی هاردکدشده‌ی `"for Milad"` می‌ساخت (نه اسم واقعی کاربر لاگین‌شده)، و چون تسک‌ها از پایپ‌لاین شخصی هرکسی می‌سازن، حساب‌های تازه/کم‌فعالیت (پایپ‌لاین خالی) عملاً پرامپت تقریباً یکسون می‌گرفتن → خروجی AI شبیه هم به‌نظر می‌رسید، با اینکه واقعاً جدا تولید و ذخیره می‌شد. رفع شد: `generate_daily_tasks()` حالا اسم واقعی کاربر (`current_user.name` / `user.name`) رو می‌گیره و توی پرامپت جایگزین می‌کنه — هم مسیر دستی (`POST /tasks/generate`) هم جاب خودکار شبانه (`auto_reports.py`).
- **باگ ۲ — تسک شخصی متن نوشته‌شده رو گم می‌کرد و به «personal» عمومی تبدیل می‌شد**: ریشه دقیق پیدا شد — `add_personal_task` توی `backend/app/routers/companies/tasks.py` عنوان (`title`) رو هیچ‌وقت روی ردیف دیتابیس ذخیره نمی‌کرد (فقط `description`)؛ پاسخ فوری API با یک override دستی قلابی درستش نشون می‌داد ولی با رفرش بعدی، چون هم `title` هم `description` (اگه خالی گذاشته بودی) خالی بودن، فرانت fallback می‌کرد به `task_type` یعنی کلمه‌ی «personal». رفع شد با اضافه‌کردن `title=data.title` به ساخت ردیف. همون الگوی باگ توی تسک‌های AI-ساخته هم بود (فقط description ذخیره می‌شد نه title واقعی) — همزمان درست شد.
- **پنل تسک‌های Dashboard حذف شد**، برگشت کامل به چیدمان تک‌ستونه‌ی قبلی (فایل `DailyTasksPanel.tsx` هم حذف شد چون دیگه استفاده نمی‌شد).
- **Identity Verification از تب اصلی پروفایل به ستون چپ منتقل شد** — یک زیربخش «Settings» جدید توی کارت پایینی سایدبار، بین لینک پروفایل عمومی و لینک Security settings، هردو (Verification + Security) با یک `borderTop` مشترک از بقیه‌ی کارت جدا شدن.
- **همپوشانی هدر موبایل با ویجت پلن**: تست دقیق با یک اکانت Trial واقعی (که ویجت پلن رو نشون می‌ده) در حالت موبایل هیچ همپوشانی‌ای نشون نداد — به احتمال زیاد این از چیدمان موقت پنل تسک‌های Dashboard (که همین دور حذف شد) ناشی می‌شد؛ با برگشت به چیدمان تک‌ستونه احتمالاً خودش رفع شده.
تست شد end-to-end با ۲ کاربر جدا (ادمین + یک اکانت Trial تازه‌ساز): تسک شخصی هرکدوم درست و مجزا ذخیره و نمایش داده شد (عنوان واقعی، نه «personal»)، پنل Dashboard دیگه نیست، پروفایل Settings-cluster درست کار می‌کنه. پیش از اون (دور هجدهم) — 🎨 فاز ۷ / مرحله‌ی ۹: **بازطراحی عمیق پروفایل** — ادغام تب‌ها، تحصیلات/تجربه، احراز هویت توی پروفایل، صفحه‌ی Security مجزا، پروفایل عمومی دو‌ستونه. لیست بلندی از اصلاحات که Milad بعد از دیدن نسخه‌ی اول خواست:
- **باگ رفع شد**: وبسایت/تلفن قبلاً ذخیره می‌شدن ولی هیچ‌جا نمایش داده نمی‌شدن (فقط مشکل نمایش بود، نه ذخیره‌سازی).
- **۳ تب (Info/Skills/Portfolio) ادغام شدن به یک تب «Profile»** با کارت‌های بخش‌بندی‌شده (Headline & Bio، Skills، Portfolio، Education، Experience، Identity Verification) — هرکدوم با دکمه‌ی مداد کوچیک کنارش که حالت نمایش/ویرایش رو toggle می‌کنه (دقیقاً الگوی آپورک). Headline محدود به ۸۰ کاراکتر، Bio محدود به ۶۰۰ کاراکتر، هردو با شمارنده‌ی زنده.
- **دو بخش کاملاً جدید — تحصیلات و تجربه‌ی کاری** (مثل آپورک): بدون migration، دقیقاً همون الگوی JSON-blob پورتفولیو (`EducationItem`/`ExperienceItem` توی `backend/app/routers/auth.py`)، هم توی تب اصلی قابل مدیریت هم خلاصه‌ش توی ستون چپ.
- **بخش احراز هویت وارد پروفایل شد**: یک کارت وضعیت (Not started/Pending/Verified/Rejected با رنگ semantic) که از `GET /marketplace/verification/me` می‌خونه، با دکمه‌ی «Complete verification» که به همون فرم کامل `/verification` می‌ره (بدون بازسازی فرم KYC، فقط entry point جدید — قبلاً بعد از verified شدن لینک ورود بهش گم می‌شد).
- **تب Security جدا شد به صفحه‌ی مستقل `/profile/security`**: شامل Personal Information (که از تب اصلی منتقل شد — location/company/website/phone حالا فقط اونجا قابل‌ویرایشن)، Publish، Change Password، Gmail، Account Info، Danger zone.
- **ستون چپ بازچینی شد**: آواتار → نام+تیک → headline (جدید) → بج‌ها → لوکیشن/کمپانی → بیوی کوتاه → مهارت‌ها → `ProfileCompletion` → خلاصه‌ی Education/Experience (جدید) → **ایمیل/تلفن/وبسایت در انتهای کارت** (طبق درخواست صریح، قبلاً بالای کارت بودن) → Member since → لینک پروفایل عمومی → لینک Security settings.
- **پروفایل عمومی (`/u/[username]`) بازطراحی شد به دو‌ستونه**: ستون چپ = هویت + ایمیل + وبسایت (**بدون تلفن**، تایید شده با Milad) + خلاصه‌ی Education/Experience + دکمه‌ی Message؛ ستون راست = ۲ تب «Portfolio» و «Posts». بخش «Reputation» قدیمی (۴ کارت آمار + لیست کامل ریویو) از این نسخه حذف شد چون Milad صریحاً گفت ستون راست فقط پورتفولیو و پست باشه — فقط خلاصه‌ی امتیاز (★) توی ستون چپ موند.
- **Endpoint جدید بک‌اند بدون نیاز به لاگین**: `GET /auth/profile/public/{username}/posts` — چون endpoint قبلی پست‌ها (`/marketplace/feed/users/{id}/posts`) پشت لاگین اجباری قفل بود و پروفایل عمومی باید برای بازدیدکننده‌ی ناشناس هم کار کنه.
- **باگ بالقوه‌ی از‌دست‌رفتن داده پیدا و رفع شد حین کار**: `PublishSection` (که Publish toggle رو مدیریت می‌کنه) قبلاً `headline`/`education`/`experience` رو توی payload خودش نمی‌فرستاد — چون `PUT /auth/profile/me` کل بلاب رو جایگزین می‌کنه، هر Publish می‌تونست این فیلدهای جدید رو خالی کنه. رفع شد قبل از اینکه به تولید برسه.
تست شد end-to-end: seed یک پروفایل کامل (headline+education+experience+portfolio+contact) → تایید بک‌اند (`GET /auth/profile/me`، `GET /auth/profile/public/{username}`، `GET .../posts` بدون توکن) → مرورگر: toggle ادیت هر بخش، ذخیره از صفحه‌ی Security بدون پاک‌شدن فیلدهای جدید، نمایش صحیح پروفایل عمومی دو‌ستونه با تب‌ها. پیش از اون (دور هفدهم) — 🎨 فاز ۷ / مرحله‌ی ۸: **بازطراحی چیدمان صفحه‌ی پروفایل، دو‌ستونه شبیه آپورک.** بعد از دیدن نسخه‌ی قبلی، Milad درست گفت که هنوز اصلاً شبیه آپورک نشده — چون تا اون‌جا فقط اجزای داخلی (گرید پورتفولیو، درصد تکمیل) رفرش شده بودن، نه **چیدمان کلی صفحه**. `/profile` قبلاً یک ستون عمودی تک‌ستونه بود (کارت هدر → نوار تب افقی تمام‌عرض → محتوا). الان یک چیدمان دو‌ستونه‌ست: **ستون چپ ثابت (sticky, ۲۸۰px)** با خلاصه‌ی هویت (آواتار، نام+تیک Verified، بج پلن/ادمین، لوکیشن/کمپانی، بیو، چیپ‌های مهارت، «Member since»، لینک «View public profile» اگه پابلیک باشه) + ویجت `ProfileCompletion` (منتقل‌شده از بالای تب Info)، و **ستون اصلی** با همون ۵ تب قبلی (Info/Skills/Portfolio/Posts/Security) دست‌نخورده — فقط نوار تب از تمام‌عرض صفحه به بالای ستون اصلی منتقل شد. هیچ الگوی «ستون چپ ثابت + ستون اصلی» قبلاً جای دیگه‌ای کدبیس نبود — این یک الگوی چیدمان جدیده که فقط همین‌جا استفاده شد. در موبایل ستون چپ بالای ستون اصلی stack می‌شه. تست شد: چیدمان دسکتاپ دو‌ستونه واقعی (نه فقط بصری تقریبی — با اندازه‌گیری DOM تایید شد: ستون چپ ۲۸۰px کنار ستون اصلی، نه زیرش)، موبایل درست stack شد بدون overflow افقی، آپلود آواتار از ستون چپ و سوییچ تب از ستون اصلی سالم، لینک پروفایل عمومی بعد از پابلیک‌کردن درست ظاهر شد. قدم بعدی: تصمیم جدید لازمه — یا ادامه‌ی همین جهت (بازطراحی چیدمان صفحات دیگه‌ی مارکت‌پلیس) یا برگشت به نقشه‌راه ۷مرحله‌ای اصلی. پیش از اون (دور شانزدهم) — 🎨 فاز ۷ / مرحله‌ی ۷ (آخرین آیتم لیست تاییدشده): **پنل تسک‌های روزانه در سمت راست Dashboard**، الهام از ستون راست آپورک. Dashboard قبلاً تک‌ستونه بود؛ الان یک پنل باریک (۲۹۰px، sticky) کنار لیست/برد شرکت‌ها نشون داده می‌شه با خلاصه‌ی تسک‌های امروز — نوار پیشرفت، حداکثر ۸ تسک با چک‌باکس قابل‌کلیک (توگل مستقیم از همون `PATCH /companies/tasks/{id}/done` که صفحه‌ی کامل `/tasks` استفاده می‌کنه، پس sync بین‌شون خودکاره)، و لینک «View all tasks». عمداً بدون دکمه‌ی افزودن/حذف/generate توی پنل — این‌ها فقط توی صفحه‌ی کامل می‌مونن تا شلوغ نشه. تغییر لایوت محدود به تغییر یک `flexDirection` و اضافه‌کردن یک لایه‌ی wrapper بود (بدون تغییر منطق فیلتر/pagination/Board drag-drop/drawer شرکت). در موبایل پنل کامل مخفی می‌شه. تست شد: توگل چک‌باکس در پنل هم UI هم بک‌اند رو آپدیت کرد (تایید شده با یک fetch مستقیم از API)، نمای Board کنار پنل بدون بهم‌ریختگی کار کرد، موبایل بدون overflow افقی پنل رو کامل حذف کرد.

**🎉 هر ۵ آیتم لیست تاییدشده‌ی Milad برای این دور از فاز ۷ کامل شد**: Quick Preview Drawer (شرکت + کاربر)، بازطراحی گرید پورتفولیو، درصد تکمیل پروفایل، و پنل تسک‌های روزانه. قدم بعدی نیاز به تصمیم جدید از Milad داره (مثلاً بازطراحی کامل چیدمان صفحه‌ی پروفایل شبیه آپورک، یا ادامه‌ی نقشه‌راه ۷مرحله‌ای اصلی: پنل ادمین، بقیه‌ی سطوح مارکت‌پلیس، حالت‌های خالی/لودینگ/موبایل، AI Search). پیش از اون (دور پانزدهم) — 🎨 فاز ۷ / مرحله‌ی ۶: **درصد تکمیل پروفایل**. Milad ۵ آیتم خواسته بود (احراز هویت، اولین پست، اولین ایمیل، پابلیک‌کردن پیج، افزودن پورتفولیو)؛ یک آیتم ششم هم اضافه شد به پیشنهاد من: **عکس پروفایل** (طبق تحلیل آپورک، یکی از مهم‌ترین فاکتورهای اعتماد). بک‌اند: به `GET /auth/profile/me` سه فیلد جدید اضافه شد (`is_verified`, `has_post`, `has_sent_email`) — هرکدوم با یک query سبک lifetime (نه period-scoped مثل `/auth/usage`)، دقیقاً با همون الگویی که تابع ادمین‌فقط `_user_activity` قبلاً استفاده می‌کرد. فرانت‌اند: کامپوننت جدید `ProfileCompletion.tsx` — نوار پیشرفت + چک‌لیست ۶ آیتمی با لینک/دکمه‌ی کوتاه کنار هر آیتم برای رفتن مستقیم به همون بخش (احراز هویت→Verification، پورتفولیو→تب Portfolio، پابلیک‌کردن→تب Security جایی که واقعاً دکمه‌ی Publish هست، پست/ایمیل→Feed/Dashboard). بالای تب Profile Info رندر می‌شه. یک باگ کوچیک حین پیاده‌سازی پیدا و رفع شد: `PublishSection` (که دکمه‌ی واقعی Publish رو داره) توی تب Security جداگونه‌ست، نه Info — یک `onPublicChange` callback بهش اضافه شد تا صفحه‌ی والد فوراً (بدون رفرش) از تغییر وضعیت public باخبر بشه. تست شد end-to-end: کاربر تازه با همه‌چی خالی → ۰٪، مرحله‌به‌مرحله (آواتار، پورتفولیو، پابلیک‌کردن) → درصد به‌درستی و زنده (بدون رفرش صفحه) بالا رفت. قدم بعدی طبق تایید Milad: پنل تسک‌های روزانه در سمت راست Dashboard. پیش از اون (دور چهاردهم) — 🎨 فاز ۷ / مرحله‌ی ۵: بازطراحی گرید پورتفولیو (فقط بصری، بدون تب Draft/Published طبق تایید صریح Milad — بدون تغییر بک‌اند). یافته‌ی مهم: منطق رندر گرید پورتفولیو در ۳ فایل جدا کپی‌پیست شده بود (Profile قابل‌ویرایش، `members/[id]` فقط‌خواندنی، `u/[username]` صفحه‌ی عمومی با تم تیره‌ی ثابت مستقل از تم سایت) — با استایل‌ها و رفتار کمی متفاوت هرکدوم. یک کامپوننت مشترک `PortfolioGrid.tsx` ساخته شد که هر ۳ حالت رو پوشش می‌ده (`editable` برای Profile، `variant="dark"` برای صفحه‌ی عمومی که رنگ‌های ثابتش رو حفظ می‌کنه، حالت ساده برای `members/[id]`)، با آیکون‌های Lucide (`Building2` جایگزین ایموجی 🏛، `Camera`/`Plus`/`X`/`ExternalLink`) به‌جای ایموجی، و یک empty-state یکدست. منطق state/آپلود/حذف/لایت‌باکس در هر صفحه دست‌نخورده موند (فقط JSX رندر گرید جایگزین شد). یک باگ کوچیک هم رفع شد: `members/[id]` قبلاً وقتی پورتفولیو خالی بود کل بخش رو مخفی می‌کرد، حالا مثل بقیه‌ی صفحات empty state نشون می‌ده. تست شد با ۳ آیتم واقعی (با عکس چندتایی، تک‌عکس، بدون عکس) روی هر ۳ صفحه — گرید، لایت‌باکس، لینک خارجی، empty state، همه سالم. قدم بعدی طبق تایید Milad: درصد تکمیل پروفایل (۵ مرحله: احراز هویت، اولین پست، اولین ایمیل، پابلیک‌کردن پیج، افزودن پورتفولیو)، بعد پنل تسک‌های روزانه در Dashboard. پیش از اون (دور سیزدهم) — 🎨 فاز ۷ / مرحله‌ی ۴ (بخش دوم): **Quick Preview Drawer** برای پروفایل کاربرها، شروع با Feed. کامپوننت مشترک و reusable `MemberPreviewDrawer.tsx` ساخته شد (برخلاف drawer شرکت که فقط داخل یک صفحه بود، این یکی قراره توی چند صفحه‌ی دیگه هم استفاده بشه: Projects، Contracts، Messages، Users، Marketplace Admin — قدم‌های بعدی). چون صفحاتی مثل Feed معمولاً فقط `user_id`+نام دارن نه کل پروفایل، drawer موقع باز شدن یک فچ سبک به `GET /marketplace/members/{id}` می‌زنه (همون endpoint صفحه‌ی پروفایل). کلیک روی اسم نویسنده‌ی پست در Feed حالا drawer رو باز می‌کنه (بدون navigate)، Ctrl/Cmd-click طبیعی به تب جدید می‌ره. محتوا: آواتار/initials، نام+تیک Verified، نوع اکانت (Hires/Takes work)، لوکیشن، بیو، چیپ‌های skills، آمار rating/completed/portfolio، دکمه‌ی «View full profile». تست شد با کاربر واقعی (دیتای واقعی، نه ساختگی) — درست رندر شد، بسته با X/Escape/کلیک بیرون کار کرد. قدم بعدی طبق تایید Milad: بازطراحی گرید پورتفولیو، بعد درصد تکمیل پروفایل (با ۵ مرحله: احراز هویت، اولین پست، اولین ایمیل، پابلیک‌کردن پیج، افزودن پورتفولیو)، بعد پنل تسک‌های روزانه در Dashboard. پیش از اون (دور دوازدهم): 🎨 فاز ۷ / مرحله‌ی ۴ (بخش اول): **Quick Preview Drawer** برای شرکت‌ها در Dashboard — الهام از UI آپورک (Milad خودش این ایده رو داد). حالا کلیک روی یک شرکت توی نمای List، به‌جای رفتن مستقیم به صفحه‌ی کامل، یک پنل از راست باز می‌کنه (بدون تغییر URL، بدون فچ جدید — چون داده‌ی ردیف از قبل توی حافظه‌ست) با خلاصه، امتیاز، وضعیت، ایمیل/قفل، و دکمه‌ی «Open full profile». لیست پشت پنل و موقعیت اسکرول/فیلترها دست‌نخورده می‌مونن. بسته می‌شه با کلیک بیرون، دکمه‌ی X، یا Escape. تست شد: باز/بسته‌شدن، تغییر status و favorite از داخل drawer (با بک‌اند sync می‌شه)، دکمه‌ی Open full profile واقعاً ناوبری می‌کنه. نمای Board و context-menu دست‌نخورده موندن (فقط List). قدم بعدی طبق تایید Milad: همین Drawer برای پروفایل فریلنسرها، بعد بازطراحی گرید پورتفولیو، بعد درصد تکمیل پروفایل (با ۵ مرحله: احراز هویت، اولین پست، اولین ایمیل، پابلیک‌کردن پیج، افزودن پورتفولیو)، بعد پنل تسک‌های روزانه در Dashboard. پیش از اون (دور یازدهم): 🎨 فاز ۷ / مرحله‌ی ۳ (بخش دوم): بازطراحی Company Detail. همون حلقه‌ی SVG امتیاز که توی Dashboard حذف شد، اینجا هم پیدا و همون‌طور حذف شد (عدد mono ساده جایگزینش شد) — سراسر اپ حالا یکدست. آیکون‌های تب (Overview/Notes/History/Generate/Emails) و دکمه‌های Generate Email/Edit از ایموجی به Lucide عوض شدن. `card` const مشترک صفحه (که خودش از قبل خوب بود، روی ۸+ بخش استفاده می‌شد) به توکن رادیوس وصل شد. تست شد با شرکت واقعی: امتیاز، تب‌ها، دکمه‌ها سالم. پیش از اون (دور دهم): 🎨 فاز ۷ / مرحله‌ی ۳ (بخش اول): بازطراحی Dashboard، فقط نمای List. هر ردیف شرکت از یک «کارت جدا» (border+radius دور هر ردیف) به یک ردیف داخل یک container واحد با خط جداکننده‌ی نازک تبدیل شد. حلقه‌ی SVG انیمیشنی امتیاز حذف شد، جایگزین با عدد ساده به فونت مونو (JetBrains Mono) و رنگ semantic. بج تگ‌ها سبک‌تر شدن (تگ‌های ثانویه دیگه پس‌زمینه ندارن). توی نوار ابزار بالای صفحه هم نمادهای یونیکد/ایموجی (⌕⊟✦☰⊞↕🔔) با آیکون‌های Lucide یکدست جایگزین شدن. نمای Board (Kanban) و صفحه‌ی Company Detail و Profile **دست‌نخورده موندن** — قدم بعدی. تست شد با دیتای واقعی: ردیف‌ها، تاگل status، ستاره‌ی favorite، سوییچ به Board — همه سالم. پیش از اون (دور نهم): 🎨 فاز ۷ / مرحله‌ی ۲: بازطراحی Sidebar. یافته‌ی مهم: Sidebar اصلاً از متغیرهای CSS استفاده نمی‌کرد — رنگ‌هاش مستقیم توی JS با `dark ? hex1 : hex2` ساخته می‌شدن، یعنی آپدیت توکن‌های مرحله‌ی ۱ هیچ اثری روی ظاهر Sidebar نداشت (دقیقاً همون چیزی که تحلیل بیرونی «خیلی سفید» توصیف کرده بود). رفع شد: Sidebar و NotificationBell الان از `var(--bg-sidebar)`/`var(--text)`/... واقعی می‌خونن، پس هر تغییر توکن آینده خودکار روش اثر می‌ذاره. آیکون‌های SVG دستی با `lucide-react` (۱۵ آیکون، stroke=1.5) عوض شدن. دکمه‌های Dark/Light و Help&Tour از دو تا pill پررنگ به یک ردیف ساده‌ی بدون پس‌زمینه (فقط هاور) تبدیل شدن. تست شد: تاگل دارک/لایت حالا واقعاً رنگ Sidebar رو عوض می‌کنه (قبلاً نمی‌کرد). پیش از اون (دور هشتم): 🎨 **فاز ۷ شروع شد.** مرحله‌ی ۱ از بازطراحی UI/UX: پایه‌ی توکن‌های دیزاین سیستم آرکون v1.0 پیاده شد — رنگ برند از بنفش/آبی قدیمی (`#4F7BF7`→`#7C3AED`) به **Cobalt** (`#3D4FE0`→`#2E3BB0`) عوض شد در کل اپ (۳۵+ فایل)، فونت از Arial به **Vazirmatn** (پشتیبانی فارسی+لاتین یکپارچه، از طریق `next/font/google`) سوییچ شد، سطوح از حالت شیشه‌ای/نیمه‌شفاف به تخت و مات تغییر کردن، و توکن‌های جدید (فاصله‌گذاری، گوشه‌ی گرد، سایه، رنگ‌های وضعیت، فونت مونو JetBrains) اضافه شدن. `lucide-react` هم نصب شد برای فازهای بعدی (آیکون یکپارچه به‌جای emoji). سند کامل سیستم دیزاین (شامل بازبینی یک تحلیل خارجی و نقشه‌راه ۷مرحله‌ای) به‌عنوان آرتیفکت جداگانه برای Milad ساخته شد. پیش از اون (دور هفتم): ✅ باگ فلش سایدبار رفع شد: آیتم‌های Projects/Feed/My Contracts/Messages با هر ریلود یک لحظه غیب و دوباره ظاهر می‌شدن چون `marketplaceEnabled` همیشه با `false` شروع می‌شد تا جواب `/auth/me` برسه. الان مثل `user`/`avatar` توی localStorage کش می‌شه. **فاز ۶ به‌طور کامل بسته شد — آماده‌ی شروع فاز ۷ (Ecosystem & Scale).** پیش از اون (دور ششم): Daily Tasks و Weekly Report حالا **خودکار** ریست/تولید می‌شن: تسک‌های روزانه هر روز ساعت ۰۰:۰۰ به وقت ایران، گزارش هفتگی هر شنبه ساعت ۰۰:۰۰ به وقت ایران — برای همه‌ی کاربران فعال، بدون نیاز به کلیک دستی. پیش از اون (دور پنجم): تیک آبی «Verified» کنار اسم کاربران احراز-هویت-شده در سراسر پلتفرم (Feed، پروفایل، پیام‌ها، پروژه‌ها، قراردادها، پنل ادمین، پروفایل عمومی). به‌علاوه ۳ باگ رفع شد: موقعیت غلط پنل ناتیفیکیشن (خارج از کادر)، از دست رفتن فوکوس هنگام تایپ در فرم Verification و Lead Hunter، و بج ناتیفیکیشن که بعد از دیدن پاک نمی‌شد. پیش از اون (دور چهارم): **۵ آیتم باقی‌مانده‌ی نقشه راه بسته شد** — از فاز ۶، Broadcast & Notification Center، Email Reputation Score، Automated Weekly Digest، و Community Feed؛ و از فاز ۵، Gmail OAuth Connect (ارسال از ایمیل خود کاربر). فاز ۶ حالا **کامل** است. پیش از اون: افکت گذار نرم بین صفحات + هاور یکپارچه، و پیش‌تر Phase 6 (Project Marketplace) با ۹ ایراد رفع‌شده بعد از تست واقعی Milad. هر ۷ فاز اصلی نقشه راه پیاده شد (تابلوی پروژه، پروپوزال، قرارداد با مایلستون، چرخه‌ی کامل پرداخت دستی، چت، ریویو، محدودیت‌های بتا)، دور دوم اضافه کرد: آپدیت زنده بدون رفرش، سیستم نوتیفیکیشن با ایمیل، احراز هویت + اطلاعات بانکی، صفحه‌ی پرداخت ادمین، پیام مستقیم، ریویو در پروفایل، و ویرایش/حذف پروژه. به‌علاوه نقشه‌ی بازار به داده‌ی ۱:۱۰m ارتقا پیدا کرد و ابزارهای مدیریتی کاتالوگ اضافه شد.

---

## 🆕 دور ششم 2026-08-24 — ریست خودکار Daily Tasks و Weekly Report

Milad متوجه شد گزارش هفتگی از تاریخ 9 جولای همچنان نمایش داده می‌شد — چون قبلاً **کاملاً دستی** بود: فقط با کلیک روی Generate تولید می‌شد و یک قفل ۷روزه‌ی *نسبی* (از آخرین کلیک) جلوی تولید دوباره رو می‌گرفت، بدون هیچ زمان‌بندی خودکاری. Daily Tasks هم مشابه بود — فقط با کلیک تولید می‌شد.

- **`services/timezone_utils.py`** (جدید) — `iran_day_start_utc()`: شروع روز تقویمی ایران (Asia/Tehran) به‌صورت UTC خام، برای مقایسه‌ی درست با ستون‌های datetime دیتابیس. قبلاً `/tasks/today` و `/tasks/generate` از نیمه‌شب UTC استفاده می‌کردن که با نیمه‌شب ایران فرق می‌کنه (ایران UTC+3:30) — یعنی تسکی که درست بعد از نیمه‌شب ایران ساخته می‌شد، چند ساعت بعد (وقتی مرز روز UTC رد می‌شد) از دید کاربر ناپدید می‌شد. رفع شد.
- **`services/auto_reports.py`** (جدید) — `run_daily_tasks(db)` و `run_weekly_reports(db)`: روی همه‌ی کاربران فعال (`is_active` و `plan_status="active"`) لوپ می‌زنن؛ کاربری که هیچ شرکتی آنلاک نکرده skip می‌شه (صرفه‌جویی در هزینه‌ی Claude). از همون منطق endpoint دستی استفاده می‌کنن (`build_and_save_report` که از `routers/companies/reports.py` استخراج شد تا دو مسیر (دستی + خودکار) از یک کد واحد استفاده کنن و از هم جدا نیفتن).
- **`services/digest_scheduler.py`** (بسط داده شد) — ۲ job جدید روی APScheduler موجود اضافه شد:
  - `daily_tasks_reset`: هر روز ساعت ۰۰:۰۰ Asia/Tehran
  - `weekly_report_reset`: هر شنبه ساعت ۰۰:۰۰ Asia/Tehran (شروع هفته‌ی ایرانی)
  - job قبلی (`weekly_digest`، ایمیل خلاصه‌ی هفتگی) دست‌نخورده موند، همچنان دوشنبه ۸ صبح UTC.
- تست شد: هم unit-level (mock کردن Claude، تایید نوشتن ردیف در `daily_tasks`/`weekly_reports`)، هم registration واقعی APScheduler (هر ۳ job با timezone `+03:30` درست ثبت شدن)، هم HTTP واقعی روی `/companies/tasks/today`.
- ⚠️ **هزینه:** این جاب‌ها روی *همه‌ی* کاربران فعال با pipeline غیرخالی هر روز/هفته یک بار Claude صدا می‌زنن — با رشد تعداد کاربران هزینه‌ی API متناسب رشد می‌کنه. اگه لازم شد، می‌شه محدودش کرد به پلن‌های خاص (مثلاً فقط Pro/Agency).

---

## 🆕 دور پنجم 2026-08-24 — تیک آبی Verified + رفع باگ‌های UI مارکت‌پلیس

- **تیک آبی Verified**: `frontend/app/components/VerifiedBadge.tsx` (کامپوننت مشترک) + `services/marketplace_access.py` → `is_verified()`/`verified_user_ids()` (بر پایه‌ی `UserVerification.status == "verified"`، همون منبعی که صفحه‌ی `/verification` استفاده می‌کنه). به تمام سریالایزرهایی که اسم کاربر رو برمی‌گردونن اضافه شد: Feed (پست/کامنت)، پروفایل عضو، پروژه‌ها، پروپوزال‌ها، قراردادها، ریویوها، چت/پیام مستقیم، `/auth/me`، جدول Users ادمین، Marketplace Admin، و پروفایل عمومی (`/u/username`).
- **باگ ۱ — پنل ناتیفیکیشن خارج از کادر**: `right:0` نسبت به یک container باریک نزدیک لبه‌ی سایدبار بود، پس پنل ۳۲۰px عمدتاً بیرون صفحه می‌افتاد. اصلاح شد به `position:fixed` با مختصات واقعی دکمه (`getBoundingClientRect`)، clamp‌شده داخل viewport.
- **باگ ۲ — از دست رفتن فوکوس هنگام تایپ** (صفحه‌ی Verification و Lead Hunter): کامپوننت‌های `Field`/`Section`/`Chip` **داخل** تابع کامپوننت صفحه تعریف شده بودن → هر رندر یک function identity جدید → React هر `<input>` رو remount می‌کرد → فوکوس هر حرف پاک می‌شد. به سطح ماژول (بیرون از کامپوننت) منتقل شدن.
- **باگ ۳ — بج ناتیفیکیشن پاک نمی‌شد**: فقط با کلیک روی هر آیتم read می‌شد. حالا باز کردن پنل خودکار همه رو read می‌کنه.

هر ۴ مورد با تست واقعی مرورگر (لاگین واقعی، تایپ واقعی، احراز هویت واقعی) تایید شد.

---

## 🆕 دور چهارم 2026-08-23 — تکمیل فاز ۶ + Gmail OAuth از فاز ۵

بررسی روی روی‌بگ اصلی (`Vision and Roadmap V04.html`) نشون داد ۴ آیتم فاز ۶ و یک آیتم فاز ۵ هیچ‌وقت پیاده نشده بودن، هرچند STATUS.md قبلاً فاز ۶ رو «تکمیل» اعلام کرده بود. این دور همه‌شون رو بست.

### ۱. Broadcast & Notification Center
- `services/notifications.py` → تابع `broadcast()` (بازاستفاده از `notify()` موجود روی لیست دلخواه کاربران).
- `routers/marketplace/admin.py` → `GET /marketplace/admin/broadcast/preview` + `POST /marketplace/admin/broadcast` — فیلتر بر اساس plan، `marketplace_beta_enabled`، و `last_login`. **کشور پشتیبانی نمی‌شه** چون روی `User` وجود نداره (فقط روی `Company`/`UserVerification` هست).
- UI در `admin/page.tsx`: فیلتر → Preview count → تایید → ارسال (دقیقاً الگوی bulk-delete موجود).

### ۲. Email Reputation Score
- بدون webhook از Resend (تنظیم نشده) — فرمول تقریبی: `score = clamp(0,100, reply_rate*100 - bounced_manual*10)`.
- جدول جدید `email_reputation_events` (`services/reputation.py`). رویداد `sent` هر بار ارسال موفق (`companies/email.py`)، `replied` وقتی کمپین به replied می‌ره (`companies/campaigns.py`)، `bounced_manual` با دکمه‌ی دستی ادمین (`POST /companies/email/{id}/mark-bounced`).
- کارت امتیاز در **صفحه‌ی پروفایل خود کاربر** (`profile/page.tsx`) — فقط وقتی حداقل یک ایمیل ارسال شده نشون داده می‌شه.

### ۳. Automated Weekly Digest
- APScheduler داخل خود پروسه (هیچ cron خارجی توی ریپو نبود) — `services/digest_scheduler.py`، استارت در `main.py` روی `startup` event موجود.
- زمان‌بندی: **دوشنبه ۸:۰۰ UTC**. محتوا: خلاصه‌ی کوتاه (تعداد ایمیل ارسالی/ریپلای هفته) نه گزارش کامل مثل صفحه‌ی Weekly Report موجود (اون جداست، AI-generated، با قفل ۷روزه‌ی خودش).
- جدول `weekly_digest_log` جلوی ارسال دوباره رو موقع ری‌استارت سرور می‌گیره. **ارسال Digest جزو سهمیه‌ی ماهانه‌ی ایمیل کاربر حساب نمی‌شه.**
- ⚠️ نیاز به `APScheduler==3.10.4` (اضافه به `requirements.txt`) — روی Railway باید نصب بشه.

### ۴. Community Feed
- ۴ جدول جدید: `mp_posts` (با soft-delete)، `mp_post_likes`، `mp_post_comments`، `mp_post_reports`.
- روتر جدید `routers/marketplace/feed.py` (الگوی استاندارد ماژول‌های مارکت‌پلیس) — پست/لایک/کامنت/ریپورت/ادیت/حذف، به‌علاوه `GET /marketplace/feed/users/{id}/posts` برای نمایش در پروفایل.
- **اولین پیجینیشن پروژه**: `services/pagination.py` (`limit`/`offset`/`has_more`) — قبلاً همه‌ی لیست‌ها `.all()` بدون صفحه‌بندی بودن.
- فرانت: صفحه‌ی `/feed` جدید، آیتم Feed در Sidebar، بخش «Posts» در `/members/[id]`، و تب «Reported Posts» در Marketplace Admin برای دیدن و حذف پست‌های ریپورت‌شده.
- دامنه‌ی نهایی طبق خواسته‌ی Milad: متن + عکس + لایک + کامنت + ریپورت + ادیت/حذف توسط خود کاربر.

### ۵. Gmail OAuth Connect (باقی‌مانده‌ی فاز ۵)
- فقط اسکوپ `gmail.send` — Archon هرگز inbox رو نمی‌خونه.
- ستون‌های جدید nullable روی `users`: `google_refresh_token_encrypted`, `google_email`, `google_connected_at` (auto-ALTER، امن روی دیتابیس موجود).
- **اولین سکرت reversible این دیتابیس** (بقیه مثل رمز عبور one-way hash هستن) — پس `services/crypto.py` با Fernet رمزنگاریش می‌کنه؛ کلید رمزنگاری فقط در env var جدید `ENCRYPTION_KEY` هست، هرگز در دیتابیس.
- روتر جدید `routers/google_oauth.py`: `/auth/google/authorize` → ریدایرکت گوگل → `/auth/google/callback` → ذخیره‌ی توکن رمزشده. `/auth/google/disconnect` برای قطع اتصال.
- نقطه‌ی سوییچ در `companies/email.py`: اگه کاربر Gmail وصل کرده **و ایمیل ضمیمه نداره** (Gmail service فعلاً attachment ساپورت نمی‌کنه)، اول با Gmail امتحان می‌شه؛ هر خطایی fallback به Resend مشترک.
- UI: کارت «Send emails from Gmail» در تب Security پروفایل — Connect/Disconnect.
- ⚠️ **نیاز به تنظیم دستی خارج از کد**: ساخت پروژه در Google Cloud Console، فعال‌سازی Gmail API، OAuth consent screen، ساخت OAuth Client (Web)، ثبت redirect URI (`{BACKEND_URL}/auth/google/callback`)، و ست‌کردن `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `ENCRYPTION_KEY` (تولید با `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`), و `BACKEND_URL` روی env محلی و Railway. **بدون این env varها، بقیه‌ی اپ عادی کار می‌کنه** — این فیچر فقط غیرفعال می‌مونه.

### تست
هر ۵ فیچر با HTTP واقعی روی سرور لوکال تست شد (نه فقط import/compile check): لاگین، ساخت پست، لایک، کامنت، ریپورت، دیدن در پنل ادمین، حذف؛ preview/send برودکست؛ خوندن reputation. فرانت‌اند هم در مرورگر واقعی چک شد (صفحه‌ی Feed، تب Security پروفایل، Admin Panel، Marketplace Admin) — بدون خطای کنسول. `tsc --noEmit` تمیز پاس شد. دیتابیس تست (`dev_smoke.db`) بعد از تست پاک شد.

⚠️ **نکته‌ی migration:** سه جدول جدید (`email_reputation_events`, `weekly_digest_log`, `mp_posts`+`mp_post_likes`+`mp_post_comments`+`mp_post_reports`) خودکار با `create_all` ساخته می‌شن، بدون نیاز به دستور دستی. ستون‌های جدید `users` (Gmail) با auto-ALTER اضافه می‌شن. **قبل از deploy روی production بک‌آپ بگیر.**

---

## 🆕 دور سوم 2026-08-23 — افکت گذار صفحات و هاور سراسری

Milad خواست جابجایی/اسکرول بین صفحات نرم و حرفه‌ای باشه و هاور روی همه‌ی دکمه‌ها و لینک‌های پلتفرم یک افکت یکپارچه داشته باشه.

- **`frontend/app/components/PageTransition.tsx`** (جدید) — کامپوننت کلاینت که `usePathname()` رو می‌خونه و `.page-enter` رو با `key={pathname}` رندر می‌کنه، تا انیمیشن فید روی *هر* ناوبری دوباره پخش بشه (قبلاً فقط یک‌بار در بارگذاری اولیه اجرا می‌شد چون در `layout.tsx` بدون remount قرار داشت).
- **`globals.css`** — `html { scroll-behavior: smooth }` برای اسکرول نرم سراسری؛ یک قانون سراسری هاور روی `a, button, [role="button"], .clickable, summary` با `transition` روی رنگ/بک‌گراند/بوردر/سایه/اپسیتی (۰.۱۸s).
- **⚠️ نکته‌ی حیاتی که باید همیشه رعایت بشه:** هرگز از `transform` یا `filter` روی `.page-enter` یا هر ancestor مشترک با Sidebar استفاده نکن — چون هرکدوم containing block جدیدی برای فرزندهای `position: fixed` (از جمله خود Sidebar) می‌سازن و باعث پرش/چسبیدن غلط ساید‌بار موقع اسکرول/ناوبری می‌شن. این باگ قبلاً یک‌بار رخ داده و رفع شده بود؛ افکت‌های جدید عمداً فقط از `opacity` و رنگ/بوردر/سایه استفاده می‌کنن، نه transform.
- تست شد: با کلیک بین صفحات (`/dashboard` → `/tasks`)، Sidebar دقیقاً در `position: fixed`, `(0,0)` باقی موند و انیمیشن `pageEnter` هر بار دوباره اجرا شد؛ `tsc --noEmit` تمیز پاس شد.

---

## 🆕 سشن 2026-08-23 — Phase 6: Project Marketplace

### چرا این معماری؟ (تصمیم بنیادی)
Milad می‌خواست یک محیط امن بین کارفرما و فریلنسر بسازه که هر دو طرف بهش اعتماد کنن — مثل قرارداد آپورک. بررسی کردیم و به این نتیجه رسیدیم:

⛔ **escrow واقعی نمی‌سازیم.** نگه‌داشتن پول شخص ثالث از نظر قانونی یعنی تبدیل شدن به «money transmitter/custodian» که مجوز EMI (اروپا) یا money-transmitter license می‌خواد. با تحریم‌ها هم Stripe Connect و مشابهش در دسترس نیست.

✅ **راه‌حل:** بسط همون الگوی «پرداخت دستی + تأیید ادمین» که از قبل برای پلن‌ها (`PaymentRequest`) داشتیم — فقط این‌بار به‌جای «پلن»، روی «مایلستون». هیچ ریسک حقوقی *جدیدی* اضافه نمی‌شه، چون از نظر قانونی همون کاری‌ه که الان انجام می‌شه. وقتی درگاه/نماد الکترونیک ایرانی آماده شد، فقط مرحله‌ی تأیید واریز خودکار می‌شه و بقیه‌ی مدل دست‌نخورده می‌مونه.

### چرخه‌ی کامل پول (قلب سیستم)
```
کارفرما رسید واریز می‌فرسته  →  ادمین تأیید می‌کنه  →  مایلستون funded
   →  فریلنسر کار رو تحویل می‌ده  →  کارفرما تأیید می‌کنه
   →  ادمین به فریلنسر پرداخت می‌کنه  →  released
   →  وقتی همه‌ی مایلستون‌ها released شدن، قرارداد خودکار completed
```
هر گذار وضعیت **دو گارد** داره: نقش (فقط کارفرما/فقط فریلنسرِ همون قرارداد) و وضعیت فعلی مایلستون. تست شد که فریلنسر نمی‌تونه قبل از تأیید فاندینگ تحویل بده، کارفرما نمی‌تونه مایلستون فاندشده رو دوباره فاند کنه، و ادمین نمی‌تونه قبل از تأیید کارفرما payout ثبت کنه.

### چی ساخته شد

| بخش | مسیر | توضیح |
|---|---|---|
| **مدل‌ها** | `models/database.py` | `Project`, `Proposal`, `Contract`, `Milestone`, `MilestonePayment`, `MilestonePayout`, `ContractMessage`, `Review` + ستون‌های جدید `User` |
| **روترها** | `routers/marketplace/` | پکیج ماژولار مثل `companies/`: `projects`, `proposals`, `contracts`, `milestones`, `chat`, `reviews`, `admin` |
| **دسترسی نقش‌محور** | `services/marketplace_access.py` | `serialize_contract()` — یک قرارداد، دو نمای متفاوت برای کارفرما و فریلنسر |
| **محدودیت بتا** | `services/marketplace_limits.py` | سقف مبلغ قرارداد با تبدیل ارز |
| **صفحات** | `app/projects`, `app/contracts`, `app/messages`, `app/marketplace-admin` | + کامپوننت‌های `ContractChat`, `MarketplaceBeta` |

### نکات مهم طراحی
- **یک اکانت، دو نقش.** به‌جای «اکانت کارفرما» و «اکانت فریلنسر» جدا، هر اکانت `account_mode` داره (انتخاب در signup، قابل سوییچ در پروفایل). این فقط تعیین می‌کنه کدوم نما اول بیاد؛ اجازه‌ی واقعی از `client_id`/`freelancer_id` روی هر قرارداد میاد. **دلیل:** اکانت جدا یعنی اعتبار و ریویوهای یک نفر بین دو هویت تقسیم بشه.
- **مارکت‌پلیس برای همه بازه**، نه opt-in. اولش پشت فلگ بود و فقط ادمین می‌دید — بخشی که کسی نمی‌بینه تست هم نمی‌شه. فلگ به‌عنوان **کلید خاموش‌کردن per-account** موند (دکمه در صفحه Users).
- **سقف قرارداد** ($۵۰۰ پیش‌فرض، قابل تغییر در Marketplace Admin). لحظه‌ی **accept** چک می‌شه چون اونجا پول متعهد می‌شه؛ پست‌کردن پروژه و پروپوزال بالای سقف آزاده. IRR با همون نرخ زنده‌ی تومان تبدیل می‌شه.
- **چت با polling نه websocket** — ساده‌تر و قابل‌اتکاتر، و ۵ ثانیه تأخیر برای یک thread مذاکره کافیه.
- **حذف کاربر با تاریخچه‌ی قرارداد بلاک شده** — رکوردهای طرف مقابل رو خراب می‌کنه. پیام می‌گه به‌جاش Disable کن.

### باگ‌ها و رخنه‌هایی که حین ساخت پیدا و رفع شد
| # | مشکل | چرا مهم بود |
|---|---|---|
| ۱ | `marketplace_beta_enabled` پیش‌فرض `False` → اکانت Pro اصلاً Projects رو نمی‌دید | فیچر عملاً فقط ادمین بود |
| ۲ | مایلستون‌ها لازم نبود با مبلغ قرارداد جمع بزنن | سقف قرارداد قابل دور زدن؛ اجزای قرارداد با جمع کلش اختلاف داشت |
| ۳ | فرم فاند هر مبلغی رو قبول می‌کرد | پرداخت کسری تأییدشده = فریلنسر طلبکار ولی مایلستون «funded» |
| ۴ | حذف کاربر روی Postgres با FK violation کرش می‌کرد | SQLite این رو چک نمی‌کنه، پس لوکال بی‌صدا رد می‌شد |
| ۵ | صفحه Messages روی موبایل مستقیم وارد گفتگو می‌شد | `useIsMobile` در اولین رندر هنوز مقدار نگرفته |
| ۶ | پنل چت فقط نصف صفحه رو پر می‌کرد | `ContractChat` سقف ثابت ۴۸۰px داشت (برای صفحه‌ی قرارداد درست بود، برای Messages نه) |
| ۷ | پنل فاند شماره کارت/پی‌پال نشون نمی‌داد | اطلاعات پرداخت فقط در endpoint **فقط-ادمین** بود |

### وضعیت تست
**۲۲۵ تست end-to-end روی HTTP واقعی در ۵ سوئیت — همه پاس** (هرکدام از پایگاه‌داده‌ی تمیز، با `reset.py` بین اجراها). پوشش: نمایش و سوییچ نقش، تابلوی پروژه، پروپوزال (+ضمیمه، +پروفایل فریلنسر)، ویرایش/حذف پروژه، قرارداد و نمای نقش‌محور، کل چرخه‌ی پول با گاردهاش، چت و پیام مستقیم با read receipt، نوتیفیکیشن‌ها، احراز هویت، ریویو/اعتبار، سقف قرارداد در چند ارز، و **همه‌ی حالت‌های منفی** (نقش اشتباه، وضعیت اشتباه، دسترسی غیرطرف قرارداد، کلید خاموش، نشت اطلاعات بانکی). تست UI هم در سه ابعاد (دسکتاپ/تبلت/موبایل) انجام شد.

---

## 🆕 دور دوم 2026-08-23 — بعد از تست واقعی Milad

فاز ۶ که بسته شد، Milad خودش به‌عنوان کارفرما و فریلنسر تست کرد و ۹ ایراد واقعی بیرون داد. همه رفع شدن:

### الف) آپدیت زنده بدون رفرش
هر پیام یا تغییر قرارداد فقط با رفرش دیده می‌شد. حالا صفحه‌ی قرارداد هر ۶ ثانیه و صفحه‌ی پروژه هر ۸ ثانیه خودشون رو تازه می‌کنن.
- **نکته:** وقتی فرمی باز باشه (فاند/تحویل) poll متوقف می‌شه، وگرنه چیزی که کاربر نصفه تایپ کرده پاک می‌شد.

### ب) سیستم نوتیفیکیشن (زنگوله)
مدل `Notification` + زنگوله در سایدبار. هر انتقال کار برای طرفی که **حالا منتظرشه** نوتیف می‌سازه: پروپوزال دریافتی/پذیرفته/رد، رسید ارسالی، مایلستون فاند/تحویل/تأیید/پرداخت، ریویو، احراز هویت.
- **دو موردی که پول منتظر ادمینه** (رسید برای تأیید، payout سررسیده) **ایمیل هم می‌فرستن** — چون بج ندیده‌شده اونجا یعنی یکی بدون پول می‌مونه.
- ساختن نوتیف هرگز باعث شکست خود عملیات نمی‌شه (payout انجام‌شده که اعلام نشده، بازهم payout انجام‌شده‌ست).

### ج) احراز هویت + اطلاعات بانکی (`UserVerification`)
پول دستی از آرکون خارج می‌شه، پس payout به نام واقعی، حساب واقعی و راه تماس نیاز داره. صفحه‌ی `/verification`: کد ملی، تلفن، آدرس، و کارت/شبا + آپلود مدرک. بنر «Verify your identity» در سایدبار تا وقتی تکمیل نشده. تب Verifications در پنل ادمین.
- **جدا از جدول `users`** نگه داشته شده چون نیمه‌ی حساسشه — فقط خود شخص و ادمین می‌بینن، هرگز در پروفایل عمومی نمیاد (تست شد).
- ویرایش رکورد تأییدشده دوباره می‌فرستدش برای بررسی، وگرنه می‌شد بعد از تأیید شماره کارت رو با کارت شخص دیگه عوض کرد.

### د) صفحه‌ی جزئیات قرارداد + پرداخت در پنل ادمین
ادمین در مرحله‌ی آخر هیچ اطلاعاتی از طرفین نداشت. حالا کلیک روی هر قرارداد: وضعیت هر مایلستون، رد پرداخت‌ها، و **وقتی payout سررسیده** اطلاعات بانکی فریلنسر با دکمه Copy + دکمه‌ی «Mark as paid» همون‌جا. قراردادهای دارای بدهی در لیست نشان‌دار می‌شن. اگه فریلنسر verified نباشه هشدار می‌ده.

### هـ) پیام مستقیم بین کاربران (`Conversation`)
چت فقط وابسته به قرارداد بود. حالا گفتگو موجودیت مستقلیه که **ممکنه** به قراردادی وصل باشه — پس هرکسی می‌تونه به هرکسی پیام بده. دکمه‌ی 💬 Message روی هر پروفایل و در جدول Users ادمین.
- گفتگوهای قراردادهای موجود موقع استارت backfill می‌شن.

### و) ریویو در پروفایل
بخش Reputation: میانگین امتیاز، **درصد رضایت**، تعداد قرارداد تکمیل‌شده، و همه‌ی کامنت‌ها با نام نویسنده — **در هر دو جهت**، پس اعتبار کارفرما هم مثل فریلنسر دیده می‌شه.

### ز) ویرایش و حذف پروژه
دکمه‌های Edit/Delete برای صاحب پروژه.
- **حذف بعد از بسته‌شدن قرارداد ممنوعه** — اون موقع فریلنسر حق داره و پروژه باید تموم یا تسویه بشه، نه ناپدید. کسانی که پروپوزال داده بودن هنگام حذف نوتیف می‌گیرن.

### ح) صفحه‌ی پروفایل داخلی (`/members/[id]`)
عمداً از `/u/[username]` جداست:
- `/u/[username]` = صفحه‌ای که کاربر انتخاب می‌کنه روی **وب عمومی** باشه، به `is_public` احترام می‌ذاره.
- `/members/[id]` = نمای **داخل مارکت‌پلیس**. چون هویت کسی که باهاش رقابت یا معامله می‌کنی نمی‌تونه اختیاری باشه — کارفرمایی که بین چند پروپوزال انتخاب می‌کنه باید آدم پشت هرکدوم رو ببینه. برای هر کاربر لاگین‌شده کار می‌کنه ولی **فقط چیزهایی که از قبل هم قابل دیدن بودن**؛ اطلاعات هویتی/بانکی اصلاً واردش نمی‌شه.

نام کاربر در این صفحات لینک شده: کارت پروپوزال، صفحه و لیست قرارداد، صندوق پیام، تابلوی پروژه، پنل ادمین، جدول Users.

### باگ‌های دیگه‌ای که در این دور رفع شد
| # | مشکل | چرا مهم بود |
|---|---|---|
| ۱ | پنل چت در صفحه Messages فقط نصف صفحه رو پر می‌کرد | `ContractChat` سقف ثابت ۴۸۰px داشت (برای صفحه‌ی قرارداد درست بود، برای Messages نه) → حالت `fill` اضافه شد |
| ۲ | صفحه Messages روی موبایل مستقیم وارد گفتگو می‌شد | `useIsMobile` در اولین رندر هنوز مقدار نگرفته؛ حالا مستقیم `window.innerWidth` اندازه گرفته می‌شه |
| ۳ | حذف کاربر روی Postgres با FK violation کرش می‌کرد | SQLite چک نمی‌کنه، پس لوکال بی‌صدا رد می‌شد. حالا بلاک با پیام «به‌جاش Disable کن» |

⚠️ **نکته‌ی migration این دور:** ستون `mp_contract_messages.contract_id` باید nullable می‌شد (برای پیام‌های مستقیم) و SQLite این کار رو درجا نمی‌تونه انجام بده — جدول بازسازی و کپی می‌شه. روی Postgres با `ALTER COLUMN` ساده انجام می‌شه. **قبل از deploy بک‌آپ بگیر.**

### نقشه و کاتالوگ (همون سشن)
- **مرزبندی دقیق‌تر:** از فایل CDN با دقت ۱:۵۰m به فایل self-hosted با دقت **۱:۱۰m** (۵ برابر دقیق‌تر)، ساخته‌شده از Natural Earth با `mapshaper` و flag `keep-shapes` تا کشورهای خیلی‌کوچیک (سنگاپور، بحرین، مالت) حین ساده‌سازی از بین نرن. حجم تقریباً همون قبلیه.
- **زوم خودکار روی کشور انتخاب‌شده** (از نقشه یا از لیست Top Markets). سقف زوم از ۸ به ۲۰ رفت — چون سنگاپور با زوم ۸ فقط ۸ پیکسل بود.
  - ⚠️ یک نسخه با «مارکر نقطه‌ای» برای کشورهای کوچیک تست شد ولی **رد شد** (ظاهر حرفه‌ای نقشه رو خراب می‌کرد). فقط مرزبندی، بدون هیچ مارکری.
- **ادغام کشورهای تکراری:** کاتالوگ هم `USA` و هم `United States` داشت (۹۲ و ۴۱۸)، هم `UAE` و هم `United Arab Emirates` (۴۳ و ۳۰۰) — یعنی یک بازار در همه‌جا که بر اساس کشور گروه‌بندی می‌کنه دو تیکه می‌شد. `services/country_normalize.py` جلوی ورودی‌های جدید رو می‌گیره (دستی، CSV، Lead Hunter)، و ابزار merge در Admin Panel تکراری‌های موجود رو یکی می‌کنه. **روی production اجرا شد.**
- **حذف دسته‌ای شرکت‌ها** در Admin Panel: فیلتر بر اساس کشور/صنعت/سایز/منبع/نام + Preview تعداد + حذف یک‌جا. بدون فیلتر، جلوی پاک‌شدن کل کاتالوگ رو می‌گیره مگر با تأیید صریح.

---

**سشن قبلی (2026-07-31، دوم) چه‌کاری کرد:**

### الف) مدل دسترسی جدید — «unlock» به‌جای «browse آزاد»
مشکل ریشه‌ای: کاتالوگ شرکت‌ها **خودِ محصوله**، ولی سقف پلن فقط جلوی *کار کردن* با شرکت رو می‌گرفت نه *دیدنش*. یعنی یک اکانت trial رایگان می‌تونست اسم و ایمیل هر ۳۰۰۰ شرکت رو ببینه. الان:
- **unlocked** = شرکتی که کاربر روش اعتبار خرج کرده (همون ردیف `user_company_state`). سقف `max_companies` دقیقاً همین‌ها رو می‌شماره، پس Pro با سقف ۵۰۰ فقط ۵۰۰ تا رو کامل می‌بینه.
- **teaser** (شرکت unlock‌نشده، اکانت سالم): اسم/کشور/شهر/صنعت/سایز/امتیاز می‌مونه — ایمیل، تلفن، وبسایت، دامنه، لینکدین، اینستاگرام، خلاصه‌ی AI و مخاطبین حذف می‌شن. اسم عمداً می‌مونه تا کاربر بتونه تصمیم بگیره اعتبارش رو کجا خرج کنه.
- **locked** (پرداخت تأییدنشده / پلن منقضی / سهمیه تمام): **همه‌چیز** ماسک می‌شه — از جمله اسم کامل شرکت (`B•• - B••••• I••••• G••••`).
- دکمه‌ی `POST /companies/{id}/unlock` یک اعتبار شرکت خرج می‌کنه و اطلاعات رو باز می‌کنه.
- فایل مرجع: `backend/app/services/access.py` (تنها جایی که این قوانین تعریف شدن).

### ب) قفل کشورها برای trial ✅
- ستون `allowed_countries` روی جدول `plan_limits` — قابل ویرایش در Admin Panel با چیپ‌های کشور واقعی کاتالوگ (`GET /auth/catalog/countries`).
- خالی = بدون محدودیت. مقدار پیش‌فرض trial: `United Arab Emirates, Saudi Arabia, United Kingdom` (دو بازار اولویت‌۱ نقشه راه + جایی که دیتای فعلی هست). یک‌بار seed می‌شه و بعدش دست ادمینه.
- فیلتر در **SQL** اعمال می‌شه نه بعدش، پس `total`، صفحه‌بندی، نقشه، آنالیتیکس و سرچ همه یک عدد می‌گن. شرکت خارج از دامنه با id هم **۴۰۴** می‌ده (نه ۴۰۳) تا وجودش لو نره.

### ج) ۹ حفره‌ی امنیتی که در بازبینی پیدا و بسته شد
| # | مشکل | وضعیت |
|---|---|---|
| ۱ | `GET /companies/export/csv` کل کاتالوگ (اسم+ایمیل+تلفن) رو به **هر** اکانت لاگین‌شده می‌داد. یعنی trial رایگان = دانلود کل دیتابیس | حالا فقط شرکت‌های unlock‌شده‌ی خود کاربر؛ ادمین کامل |
| ۲ | سقف پلن فقط engagement رو محدود می‌کرد نه دیدن دیتا | مدل unlock (بند الف) |
| ۳ | `PATCH /companies/{id}` — هر ممبر می‌تونست فیلدهای **کاتالوگ مشترک** (اسم، ایمیل، امتیاز) رو برای همه عوض کنه | فقط ادمین |
| ۴ | `update_campaign` اجازه می‌داد ایمیل `sent` رو به `draft` برگردونی → سهمیه‌ی ایمیل ریست می‌شد (ارسال نامحدود رایگان) | برگشت ممنوع؛ فقط sent→replied |
| ۵ | فلگ‌های `ai_search` / `weekly_report` / `market_map` در `/auth/me` تبلیغ می‌شدن ولی **هیچ‌جا چک نمی‌شدن** — همه‌ی پلن‌ها همه‌چیز داشتن | `require_feature()` واقعاً enforce می‌کنه |
| ۶ | `get_current_user` روی انقضا ۴۰۳ می‌داد → کاربر منقضی به **صفحه‌ی پرداخت هم دسترسی نداشت** (نمی‌تونست پول بده!) | انقضا به `require_active_plan` منتقل شد؛ مسیر بیلینگ باز موند |
| ۷ | `tasks/generate` و `report/weekly` کل کاتالوگ رو به Claude می‌دادن و خلاصه‌شو برمی‌گردوندن — راهی برای خوندن ردیف‌های پرداخت‌نشده | فقط شرکت‌های unlock‌شده |
| ۸ | تولید ایمیل با AI بی‌نهایت و رایگان بود (هزینه‌ی توکن Claude) | نیاز به unlock بودن شرکت + سهمیه‌ی ارسال باقی‌مانده |
| ۹ | `POST /auth/users` (ادمین) پنجره‌ی پلن ست نمی‌کرد → کاربر ساخته‌شده هیچ‌وقت منقضی نمی‌شد | `activate_plan()` صدا زده می‌شه |

### هـ) سیستم امتیازدهی بازنویسی شد (`backend/app/services/scoring.py`)
**مشکل فرمول قدیمی:** امتیاز = «چقدر فیلد پر کردیم» نه «چقدر احتمال داره مشتری بشه». یعنی `ایمیل +۲۵، سایت +۱۰، لینکدین +۱۰، سایز، صنعت +۲۰، خلاصه +۵`. نتیجه‌ش این بود که یک شرکت ۵۰۰ نفره‌ی لندنی با تیم داخلی ۳بعدی که فقط `info@`ش رو پیدا کرده بودیم، **بالاتر** از یک استودیوی ۱۵ نفره‌ی دبی که تازه جایزه برده و دنبال ویژوالایزر می‌گرده امتیاز می‌گرفت.

**مدل جدید — ۵ محور وزن‌دار، جمعاً ۱۰۰:**
| محور | وزن | چی رو می‌سنجه |
|---|---|---|
| **Need** | ۲۲ | اصلاً رندر سفارش می‌دن؟ (دولوپر ۲۲ > معماری ۲۰ > اینتریور ۱۹ > … > استودیوی arch-viz ۹ چون رقیبه) |
| **Outsourcing** | **۲۶** | برون‌سپاری می‌کنن؟ **مهم‌ترین محور.** small (۳–۲۰ نفر) ۲۰ · medium ۱۶ · solo ۱۳ · large ۹. + «در حال استخدام ویژوالایزر» ۶+، «تیم داخلی نداره» ۵+ |
| **Timing** | ۲۰ | دلیلی برای جواب‌دادن *همین ماه* دارن؟ استخدام ۹ · پروژه‌ی جدید ۶ · جایزه ۶ · نمایشگاه ۵ · سرمایه ۴ |
| **Market** | ۱۷ | منطقه چقدر پول می‌ده؟ امارات/عربستان/قطر ۱۷ · اسکاندیناوی/UK ۱۵ · آلمان/هلند/آمریکا ۱۳ · اسپانیا/فرانسه ۱۱ · … |
| **Reach** | ۱۵ | اصلاً می‌تونیم ایمیل بزنیم؟ ایمیل شخصی ۹ > `info@` ۷ > بدون ایمیل ۰ · سایت +۳ · لینکدین +۲ |

به‌علاوه `style_fit` از AI (‎−۸ تا +۸): چقدر کارشون به سبک Armila می‌خوره و آیا رندرهای فعلی‌شون ضعیفه.

**نتیجه‌ی تست:** استودیوی ۱۵نفره‌ی دبی که ویژوالایزر می‌خواد → **۹۸/A**. استودیوی ۸نفره‌ی کپنهاگ با شورت‌لیست جایزه → **۸۶/A**. دولوپر ریاض → **۷۶/B**. شرکت ۵۰۰نفره‌ی لندنی → **۵۶/C**. و همون کیسی که فرمول قدیمی بالاترین امتیاز رو می‌داد (همه‌ی فیلدها پر، صنعت نامشخص، بزرگ) → **۳۶/D**.

هر امتیاز با **breakdown محور‌به‌محور** ذخیره می‌شه (ستون `companies.score_breakdown`) و در UI با دکمه‌ی «Why this score?» قابل دیدنه — عدد قابل بحثه نه جادویی. وزن‌ها همه constant های بالای فایل‌ان و راحت قابل تنظیم؛ بعد از تغییر، دکمه‌ی Recalculate Scores در Admin Panel کل کاتالوگ رو دوباره امتیاز می‌ده.

**نکته‌ی طراحی:** امتیاز رو **ما** حساب می‌کنیم نه Claude. Claude فقط *واقعیت* برمی‌گردونه (سایز، سگمنت، سیگنال‌ها، style fit) و فرمول قطعیه. یک عدد ۰–۱۰۰ که مدل تولید کنه بین اجراها نوسان داره و قابل کالیبره‌کردن نیست.

### د) Lead Hunter — صفحه‌ی جدید `/discovery` (فقط ادمین)
جایگزین پنل کوچک AI Lead Discovery داخل Admin Panel:
- **۵۳ منبع در ۹ گروه** به‌جای «گوگل سرچ»: نشریات و دایرکتوری‌ها (ArchDaily, Dezeen, Architizer, Archello, Divisare, World-Architects…)، **شورت‌لیست جوایز** (WAF, Dezeen Awards, Architizer A+, Mies, RIBA, AIA, Cityscape)، **رجیستری‌های ملی معماری** (RIBA/ARB, AIA, BAK/BDA, CSCAE, BNA, Nordic, Ordre FR, رجیستری مشاوران دبی/عربستان/قطر، اتاق معماران ترکیه)، **نمایشگاه‌ها و لیست غرفه‌داران** (Big 5, Cityscape, MIPIM, Expo Real, Salone, BAU/Batimat, Downtown Design)، پلتفرم‌های پورتفولیو (Behance, ArtStation, CGarchitect, LinkedIn, Instagram, Houzz)، نقشه و لیستینگ محلی، **دولوپرها و پورتال‌های مناقصه**، **آگهی‌های استخدام** (قوی‌ترین سیگنال خرید)، و رجیستری‌های شرکتی (Companies House, OpenCorporates, Crunchbase).
- فیلدهای عملیاتی: کشور/شهر، ۱۳ نوع کسب‌وکار، ۱۳ نوع پروژه، سایز شرکت، زبان سایت، **۸ سیگنال خرید** (در حال استخدام ویژوالایزر، تازه جایزه برده، پروژه‌ی جدید اعلام کرده، غرفه‌دار نمایشگاه آینده، تازه سرمایه گرفته، رندرهای سایتش قدیمیه، تیم داخلی ویژوال نداره، فعال در شبکه‌های اجتماعی)، سقف امتیاز، الزام وبسایت/ایمیل، و بریف آزاد.
- **جریان ۳ مرحله‌ای برای مدیریت توکن** — این مهم‌ترین بخش طراحیه:
  1. **Scout** (ارزان): فقط اسم، سایت، شهر، کشور، و منبع. بدون بازکردن سایت‌ها، بدون پیداکردن ایمیل، بدون امتیازدهی. `effort: low` و thinking خاموش، چون این مرحله فهرست‌سازیه نه قضاوت. تا ۴۰ شرکت.
  2. **Research & score** (گران): فقط روی مواردی که **شما تیکشون رو نگه داشتید**. سایت هر شرکت باز می‌شه، ایمیل/لینکدین/اینستاگرام/سایز/سگمنت پیدا می‌شه، سیگنال‌های خرید با شواهد تأیید می‌شن، و امتیاز حساب می‌شه. adaptive thinking روشن. **دسته‌ای ۶تایی** فرستاده می‌شه نه یکی‌یکی. سقف ۳۰ تا در هر کلیک.
  3. **Add**: دوباره تیک می‌زنید و فقط انتخاب‌شده‌ها به کاتالوگ اضافه می‌شن.
  
  منطقش ساده‌ست: بیشترِ چیزی که پیدا می‌شه رد می‌شه، پس تحقیق روی همه‌شون از اول = پول دورریختن. تحقیق ۲۵ شرکت برای نگه‌داشتن ۶ تا، گرون‌ترین راهه.
- **مصرف توکن هر مرحله ثبت و نمایش داده می‌شه** (ستون‌های `input_tokens`/`output_tokens` روی `discovery_runs`) — هم بالای صفحه در طول کار، هم در تب History. پس می‌تونی ببینی scout چقدر گرفت و enrich چقدر.
- هر لید با **`source` + `source_url` + `evidence` + `confidence`** برمی‌گرده — بدون citation دور ریخته می‌شه. ایمیل و سایت قبل از ذخیره قابل ویرایشن.
- امتیاز **سمت سرور دوباره حساب می‌شه** موقع ذخیره — عددی که مرورگر پس می‌فرسته نادیده گرفته می‌شه.
- **Saved hunts** (پریست قابل اجرای مجدد) + **History** (مرحله، چند تا، چند توکن) — تا بفهمی کدوم منبع بازدهی داره.
- سرویس Claude به `web_search_20260209` + `web_fetch_20260209` (با dynamic filtering) ارتقا پیدا کرد.
- افزودن منبع جدید فقط ویرایش `backend/app/services/discovery_sources.py` است — UI خودکار می‌گیره.

### و) رفع باگ production و ساده‌سازی صفحه‌ی Lead Hunter *(2026-07-31، سوم)*
- **باگ:** `Scout failed: Messages.create() got an unexpected keyword argument 'output_config'`. علت: `requirements.txt` روی `anthropic==0.28.0` پین بود (میانه‌ی ۲۰۲۴) و اون نسخه `output_config` و `thinking` رو نمی‌شناسه. venv لوکال ۰.۱۱۲.۰ داشت برای همین لوکال خطا نمی‌داد. → پین به `anthropic==0.112.0` و **هر دو شکل پارامتر (scout و enrich) با فراخوانی واقعی روی API تأیید شد**. ⚠️ نیاز به redeploy روی Railway داره تا نصب بشه.
- **همه‌ی فیلدها اختیاری بودن ولی UI این رو نمی‌گفت.** صفحه بازطراحی شد با progressive disclosure:
  - بالای صفحه یک فیلد اصلی: **«What are you hunting for?»** — توضیح به زبان ساده. همین یکی برای اجرا کافیه، و حتی خالی هم قابل اجراست.
  - **۴ پریست یک‌کلیکی**: «در حال استخدام ویژوالایزر»، «دولوپرهای خلیج در حال لانچ»، «شورت‌لیست جوایز»، «استودیوهای کوچک بدون تیم داخلی».
  - بقیه‌ی فیلترها رفتن زیر یک بخش **جمع‌شده‌ی «Narrow it down — All optional»** با ۵ زیربخش (Where / Who / Signals / Sources / Rules) که هرکدوم وقتی بسته‌ان خلاصه‌ی چیزی که ست شده رو نشون می‌دن.
- **گزینه‌های Rules حالا واقعاً کار می‌کنن**: «باید سایت داشته باشه» در مرحله‌ی scout اعمال می‌شه، و «باید ایمیل داشته باشه» + «حداقل امتیاز» در مرحله‌ی enrich — شرکت‌هایی که رد می‌شن **حذف نمی‌شن** بلکه با برچسب نارنجی علامت می‌خورن و تیکشون از اول برداشته‌ست (تصمیم با شماست).

### ز) هزینه‌ی هانت ۳۰ برابر کم شد + مسیر رایگان با اشتراک *(2026-07-31، چهارم)*
**اندازه‌گیری واقعی (نه تخمین):** یک Scout واقعی برای ۵ شرکت و ۱ منبع → **$۰.۵۸**. تفکیکش: ۱۲۴,۹۳۸ توکن ورودی + ۱۶ سرچ ($۰.۱۶). پرامپت خود ما فقط ۱,۵۶۳ توکن بود — **۹۹٪ هزینه از تزریق نتایج سرچ به context بود**، نه از مدل. تعمیم به ۳۰۰۰ شرکت نقشه راه: **$۷۰۰–۱۰۰۰**. غیرقابل‌قبول.

**راه‌حل الف — مسیر رایگان با اشتراک Claude خود Milad:**
- کامند `/hunt` در `.claude/commands/hunt.md`. لوکال با Claude Code اجرا می‌شه (تحت اشتراک ماهانه، **صفر هزینه‌ی API**)، وب رو می‌گرده، و `hunt-output.csv` با فرمت import آرکون می‌سازه.
- ⛔ **نمی‌شه سرور رو به اشتراک claude.ai وصل کرد** — API key نمی‌ده و استفاده از session token خلاف شرایط استفاده‌ست. ولی چون Lead Hunter فقط ادمینه و کاتالوگ یک‌بار ساخته می‌شه، اصلاً لازم نیست داخل سرور باشه.
- `import/csv` ارتقا پیدا کرد: dedupe با **نام** هم (قبلاً فقط دامنه بود، پس یک استودیو از دو لیست مختلف دوبار وارد می‌شد)، و ستون‌های جدید `signals` / `style_fit` / `evidence` / `source` تا ردیف import‌شده **همون امتیازی رو بگیره که در اپ می‌گرفت**. تست: ردیف کامل ۸۹/A، ردیف خالی ۲۵/D، تکراری skip.

**راه‌حل ب — مسیر داخل اپ، ۳۰ برابر ارزان‌تر:**
- `backend/app/services/websearch.py` — سرچ از **Brave** (ماهی ۲۰۰۰ رایگان) یا **Serper** ($۱/۱۰۰۰)، به‌علاوه‌ی fetch صفحات با httpx و استخراج ایمیل از `mailto:` و متن.
- **Scout** حالا خودش سرچ می‌کنه و فقط **عنوان + لینک + ۲۲۰ کاراکتر خلاصه** به مدل می‌ده (به‌جای ۱۲۰ هزار توکن صفحه‌ی کامل)، و از **Haiku 4.5** استفاده می‌کنه چون این مرحله استخراج ساختاره نه استدلال.
- **Enrich** صفحات شرکت رو خودمون می‌گیریم (home + `/contact` اگه ایمیل پیدا نشد)، تا ۵ هزار کاراکتر ترانکیت می‌کنیم، بعد به Sonnet می‌دیم.
- کوئری‌ها **دستی** ساخته می‌شن نه با مدل (`build_search_queries`) — با `site:` فیلتر روی دامنه‌ی منابع، که همون چیزیه که فراتر از بیست استودیوی معروف می‌ره.
- **بدون کلید هم کار می‌کنه** — به مسیر گرون Anthropic برمی‌گرده و صفحه یک بنر هشدار با هزینه‌ی واقعی نشون می‌ده.
- **انتخاب موتور سرچ در خود صفحه** (`search_provider`): Serper / Brave / Claude built-in. هر ایندکس شرکت‌های متفاوتی پیدا می‌کنه، پس این فقط اهرم هزینه نیست — اهرم کیفیته. اگه یک موتور شرکتی رو که می‌دونی هست پیدا نکرد، یکی دیگه رو امتحان کن. فقط گزینه‌هایی نشون داده می‌شن که کلیدشون واقعاً ست شده.
- ⚠️ **`CLAUDE_API_KEY` دست نمی‌خوره.** Serper فقط *سرچ* می‌کنه؛ Claude همچنان تولید ایمیل، تحقیق شرکت، گزارش هفتگی، تسک‌های روزانه و استخراج داده در هانت رو انجام می‌ده. این دوتا مکمل‌ان نه جایگزین.
- ⚠️ برای فعال‌سازی: **`SERPER_API_KEY`** روی Railway ست بشه — از `serper.dev`، **۲,۵۰۰ سرچ رایگان بدون نیاز به کارت اعتباری**. (جایگزین: `BRAVE_SEARCH_API_KEY`، ولی Brave پلن رایگان نام‌دار نداره — فقط $۵ کردیت ماهانه و کارت اعتباری اجباریه.)

### ح) نقشه، heat، سایز شرکت و فیلتر جدید *(2026-07-31، پنجم)*

**۱. باگ heat — هیچ‌وقت محاسبه نمی‌شد.** `heat_level` فقط یک ستون با مقدار پیش‌فرض `"cold"` بود؛ **هیچ کدی در پروژه هرگز مقدارش رو حساب نمی‌کرد.** پس هر شرکتی که import یا هانت می‌شد تا ابد cold می‌موند و فیلتر hot/warm تزئینی بود.
- مدل جدید در `services/scoring.py` → `heat_for()`. **heat و score دو سوال متفاوت می‌پرسن:** score می‌پرسه «اصلاً باید به اینا ایمیل بزنیم؟»، heat می‌پرسه «الان چیزی در جریانه؟»
  - **hot**: جواب دادن / جلسه گذاشتن / مشتری شدن — یا امتیاز ≥۷۰ + یک سیگنال فوری (استخدام ویژوالایزر، پروژه‌ی جدید، جایزه، نمایشگاه)
  - **warm**: ایمیل زدیم و منتظریم — یا امتیاز ≥۶۵ یا هر سیگنال فوری
  - **cold**: بقیه · و `archive` همیشه cold
  - **قانون کهنگی:** ایمیل زده‌شده که ۳۰ روز بی‌جواب مونده، دوباره cold می‌شه. بدون این، هر چیزی که یک‌بار تماس گرفته شده تا ابد warm می‌موند.
- **بدون نیاز به فشار دادن دکمه کار می‌کنه** — heat موقع خواندن محاسبه می‌شه. نکته‌ی ظریف: `"cold"` هم مقدار پیش‌فرض ستونه هم می‌تونه انتخاب عمدی کاربر باشه و این دوتا قابل تفکیک نیستن؛ پس هر چیزی که کاربر **فعالانه** ست کرده (hot/warm) محترمه، ولی `cold` ذخیره‌شده دوباره محاسبه می‌شه.
- **دکمه‌ی «Recalculate Heat» در Admin Panel** — مقادیر ذخیره‌شده‌ی پایپ‌لاین خودت رو به‌روز می‌کنه و شمارش hot/warm/cold رو گزارش می‌ده. فقط ردیف‌های موجود رو دست می‌زنه، هیچ‌وقت ردیف جدید نمی‌سازه (چون ساختن ردیف = خرج کردن اعتبار).
- دو باگ ظریف SQL که حین تست پیدا شد: (۱) `signals` روی اکثر ردیف‌ها NULL بود و `NULL ILIKE …` می‌شه NULL نه false، که کل AND/OR اطرافش رو مسموم می‌کرد و ردیف‌ها رو بی‌صدا از **همه‌ی** دسته‌ها حذف می‌کرد؛ (۲) hot و warm جدا تست می‌شدن، پس یک شرکت hot در نتایج warm هم ظاهر می‌شد. حالا تست می‌کنه که **شمارش فیلتر دقیقاً با چیزی که لیست نشون می‌ده یکی باشه**.

**۲. امتیازدهی بر اساس تعداد نفرات واقعی.** ستون جدید `companies.employee_count`. محور Outsourcing حالا به‌جای ۴ سطل، یک منحنی نرم روی تعداد واقعیه:
| نفرات | امتیاز Outsourcing |
|---|---|
| ۱–۲ | ۱۳ |
| ۳–۲۰ | **۲۰** (نقطه‌ی شیرین) |
| ۲۱–۵۰ | ۱۸ |
| ۵۱–۱۰۰ | ۱۴ |
| ۱۰۱–۲۵۰ | ۱۱ |
| ۲۵۱–۵۰۰ | ۹ |
| +۵۰۰ | ۶ |

**شرکت‌های بزرگ در لیست می‌مونن** — پروژه و بودجه‌ی واقعی دارن — فقط پایین‌تر: یک شرکت ۲۰۰۰ نفره **۵۷/C** می‌گیره نه صفر. منحنی شیب داره نه پرتگاه، پس یک دفتر ۲۵۰ نفره هنوز بالاتر از یک ناشناخته‌ست. روی کارت هر شرکت یک **بَج سایز با تعداد نفرات** («Large · 450») نشون داده می‌شه، چون وقتی امتیاز پایینه باید دلیلش دیده بشه.

**۳. ستون `companies.signals`.** باگی که در ضمن پیدا شد: `Recalculate Scores` امتیاز رو فقط از فیلدهای خام Company حساب می‌کرد، پس **سیگنال‌های هر شرکت هانت‌شده رو بی‌صدا دور می‌ریخت** و بهترین لیدها می‌افتادن پایین لیست. حالا سیگنال‌ها ذخیره می‌شن، در چیپ‌های زرد روی کارت دیده می‌شن، و re-score حفظشون می‌کنه. `Recalculate Scores` هم حالا سایز رو با تعداد نفرات هماهنگ می‌کنه (وگرنه کارت می‌تونست «Large · 14» بنویسه).

**۴. Market Map بازنویسی شد:**
- **جغرافیای دقیق‌تر** — از `countries-110m` به **`countries-50m`** (۵ برابر جزئیات مرزی).
- **اسم و تعداد شرکت روی هر کشور** نوشته می‌شه — فقط کشورهایی که دیتا دارن، تا نقشه به دیوار اسم تبدیل نشه. متن با زوم **معکوس مقیاس** می‌شه تا اندازه‌ش روی صفحه ثابت بمونه، و halo داره تا روی هر رنگی خونا باشه.
- **کشور انتخاب‌شده کهربایی می‌شه** با حاشیه‌ی ضخیم — از هر آبی رنگ‌بندی چگالی جدا تشخیص داده می‌شه.
- **تطبیق نام کشورها** بازنویسی شد: نرمال‌سازی (حروف کوچک، حذف علائم و اکسان) + جدول ۳۰ نامی برای موارد واقعاً متفاوت. قبلاً فقط ۷ تا بود و کشورها بی‌صدا از نقشه می‌افتادن.
- **`LABEL_OVERRIDE`** برای کشورهایی که مرکز هندسی‌شون بی‌معنیه — فرانسه به‌خاطر گویان فرانسه لیبلش وسط اقیانوس اطلس می‌افتاد.
- رنگ‌بندی چگالی به مقیاس **جذر** تغییر کرد: با چند کشوری که بیشتر کاتالوگ رو دارن، مقیاس خطی بقیه رو از «خالی» غیرقابل‌تشخیص می‌کرد.
- زوم لیبل‌ها در state جدا از prop کنترل‌شده‌ی نقشه نگه‌داری می‌شه، وگرنه نوشتن در اون حین اسکرول باعث پرش نقشه می‌شد.

**۵. فیلتر «Recently added»** به مرتب‌سازی داشبورد اضافه شد. **در ضمن یک باگ واقعی رفع شد:** مرتب‌سازی کلاً سمت **مرورگر** انجام می‌شد، یعنی «مرتب‌سازی بر اساس امتیاز» روی ۳۰۰۰ شرکت در عمل یعنی «این ۲۰ تا رو مرتب کن». حالا کل مرتب‌سازی در SQL انجام می‌شه (`sort` + `sort_dir`) و کل نتیجه رو پوشش می‌ده.

### ط) اصلاح نقشه و پیام Recalculate Heat *(2026-07-31، ششم — بر اساس فیدبک واقعی روی production)*

**۱. Recalculate Heat باگ نداشت، ولی پیامش گمراه‌کننده بود.** با ۹۶ شرکت در کاتالوگ، دکمه گفت «۵ company recalculated». علتش: `heat_level` روی جدول `user_company_state` ذخیره می‌شه و اون ردیف فقط وقتی ساخته می‌شه که با شرکت درگیر بشی (status/favorite/unlock). با فقط ۵ ردیف state، فقط ۵ تا چیزی برای بازنویسی داشتن — بقیه heatشون **زنده و درست** موقع نمایش محاسبه می‌شه (نیازی به دکمه نداره). حالا endpoint شمارش **کل کاتالوگ** رو برمی‌گردونه (نه فقط ردیف‌های نوشته‌شده)، و پیام واضحه: «🔥X 🌤Y ❄️Z across all 96 companies — 5 in your pipeline, N changed».

**۲. Market Map — لیبل‌های همیشه‌روشن حذف شد، Tooltip روی هاور جایگزین شد.** مشکل قبلی: اسم کشورها (مثل Kuwait/Iraq/Qatar/UAE در خلیج فارس) روی هم می‌افتادن و نامفهوم بود. راه‌حل حرفه‌ای‌تر (همون کاری که نقشه‌های market intelligence واقعی می‌کنن): به‌جای متن دائمی روی نقشه، حالا:
- **هاور روی هر کشور** — حتی اونایی که هیچ شرکتی ندارن — یک tooltip دنبال ماوس نشون می‌ده: اسم کشور + «N companies» یا «No companies yet».
- رنگ چگالی (choropleth) همچنان هست برای دید کلی سریع، پنل «Top Markets» کنار نقشه رتبه‌بندی کامل رو نشون می‌ده، و کلیک روی کشور جزئیات کامل رو باز می‌کنه.
- این باعث شد نقشه دیگه هیچ‌وقت شلوغ/روی‌هم نشه، صرف‌نظر از اینکه چند کشور نزدیک هم دیتا داشته باشن.

**تست:** ۳۲ تست end-to-end روی HTTP واقعی نوشته و اجرا شد (ماسک‌شدن، قفل کشور، خرج اعتبار unlock، CSV، ادیت کاتالوگ، فلگ فیچرها، بلاک pending/expired، ضدتقلب سهمیه، دسترسی Lead Hunter) — **۳۲/۳۲ پاس**. بیلد فرانت‌اند تمیز.

### ح) Playbook جمع‌آوری لید + ساختار فایل هر کشور *(2026-07-31، پنجم)*
- **مشکل ساختار:** `hunt-output.csv` مسطح تا ۳۰۰۰ ردیف دوام نمی‌آورد — چک تکراری غیرقابل‌اتکا و فایل غیرقابل‌خواندن می‌شد. → `leads/<country>.csv` (هر کشور یک فایل، هر اجرا append). ۲۵ ردیف موجود بین ۷ فایل تقسیم شد.
- **`leads/PLAYBOOK.md`** — راهنمای عملیاتی که `/hunt` هر بار قبل از سرچ می‌خواند: ۱۴ سگمنت قابل شکار (دولوپرها با ۲۲ امتیاز **بالاتر از استودیوهای معماری** با ۲۰ — قبلاً فقط دنبال معماری می‌گشتیم)، کاتالوگ منابع مرتب‌شده بر اساس بازده **اندازه‌گیری‌شده**، فیلترهای رد صلاحیت، و خط لوله‌ی اعتبارسنجی.
- **دو کشف که روش کار را عوض کرد:**
  1. **لیستیکل‌های کیوریت‌شده** (Archello «۲۵ بهترین شرکت معماری در \<کشور\>»، Architizer A+List با ۲۲۰ شرکت) → **۲۵ تا ۲۲۰ اسم از یک صفحه**، در برابر ~۱ اسم از یک مقاله‌ی پروژه. پربازده‌ترین منبع کشف‌شده.
  2. **سایت‌های دایرکتوری ۴۰۳ می‌دهند** (Archello، ASID، AIA Australia، Dezeen Jobs) ولی محتوایشان **از طریق سرچ در دسترس است**؛ سایت خود شرکت‌ها بلاک نیست. پس: دایرکتوری را سرچ کن، روی دامنه‌ی خود شرکت تأیید کن.
- **اعتبارسنجی ایمیل در ۳ لایه:** (۱) آدرس باید روی سایت خود شرکت خوانده شده باشد — آدرس از RocketReach/Lusha/ZoomInfo **پذیرفته نمی‌شود**؛ (۲) دامنه‌ی ایمیل باید با دامنه‌ی سایت بخواند؛ (۳) **چک واقعی MX** با `Resolve-DnsName` — بدون رکورد MX، ایمیل پاک می‌شود (hard bounce به اعتبار دامنه‌ی ارسال آسیب می‌زند). صحت مالباکس چک نمی‌شود (نیاز به سرویس پولی دارد).
- **اولویت کشورها بازوزن‌دهی شد:** نرخ بازار به‌تنهایی آمریکا را در Tier 3 می‌گذاشت، ولی آمریکا ~۱۹٬۰۰۰–۲۷٬۰۰۰ شرکت معماری دارد که **۷۵٪ زیر ۱۰ نفرند** (دقیقاً پروفایل هدف) در برابر ~۳۵۰ شرکت قطر با نرخ بالاتر. فرمول درست: **نرخ × اندازه‌ی استخر × دسترسی‌پذیری**. Tier S = آمریکا ۵۰۰ · امارات ۳۵۰ · بریتانیا ۳۵۰ · عربستان ۳۰۰.
- **مایلستون‌ها:** M1 ۱٬۵۰۰ (Tier S) → M2 ۲٬۴۳۰ (+A) → M3 ۳٬۲۵۰ (هدف اول رد می‌شود) → M5 ۶٬۰۰۰+. با ۴ اجرا در روز ≈ **۳۳ روز** تا ۳۰۰۰، نه ۶ ماه.

**سشن قبلی (2026-07-31، اول):** رفع سه باگ UI — علت ریشه‌ای پرش ساید‌بار (`transform` روی `.page-enter` که containing block المان‌های `fixed` رو می‌شکست)، دکمه‌ی Email در Users (fallback کپی به کلیپ‌بورد)، و بازطراحی چیدمان Analytics. **هر سه توسط Milad تست و تأیید شدن.**

---

## ۱. معرفی پروژه (خلاصه‌ی خودایستا)

Archon یک CRM/Business Development OS برای استودیوهای معماری و visualization است، ساخته‌شده توسط Milad Rostami (صاحب استودیوی Armila Design، مادرید، اسپانیا). امروز یک ابزار داخلی outreach است؛ چشم‌انداز بلندمدت تبدیل آن به یک اکوسیستم/marketplace کامل صنعت arch-viz (شبیه ترکیبی از LinkedIn + Upwork + Behance برای این نیچ) است.

**Stack:**
- Backend: FastAPI + SQLAlchemy، SQLite (local) / PostgreSQL (production، از طریق `DATABASE_URL`)
- Frontend: Next.js 14 App Router + TypeScript، inline styles
- AI: Claude API (`claude-sonnet-4-6`) برای تولید ایمیل، خلاصه شرکت، گزارش هفتگی، و Research واقعی با web_search
- مسیر پروژه: `C:\Users\Milad Rostami\archon`
- ریپو: `github.com/MiladRostami9999-source/archon`

---

## ۲. فاز فعلی: ✅ Phase 6 تکمیل شد — آماده‌ی Phase 7

> **خلاصه‌ی وضعیت:** Phase 4 (Deploy)، Phase 5 (SaaS Launch) و Phase 6 (Project Marketplace) از نظر کدنویسی **کاملاً** بسته‌ان — همه‌ی ۱۲ آیتم فاز ۶ (شامل Broadcast Center، Community Feed، Email Reputation Score، Weekly Digest که تا 2026-08-23 باز مونده بودن) پیاده‌سازی شدن.
> کاتالوگ روی **۴٬۰۱۹ شرکت در ۴۷ کشور** ایستاده (هدف اول ۳۰۰۰ رد شد ✅).
> کار باقی‌مانده بیشتر **عملیاتی** است تا کدنویسی: تنظیم دستی Gmail OAuth (Google Cloud Console)، تست production مارکت‌پلیس، ادامه‌ی جمع‌آوری لید — و شروع فاز ۷.

### تاریخچه‌ی Phase 4 (برای مرجع)

### تمام‌شده (بدون نیاز به Deploy)
- Landing Page (دوزبانه EN/FA با RTL، تم دارک/لایت، workflow navigation)
- PostgreSQL dual-mode + `migrate_to_postgres.py` آماده اجرا
- Public Profile System (`/u/username`)
- Onboarding Tour (spotlight واقعی، دوزبانه)
- Direct Email Sending (SMTP Gmail، پیوست، reply tracking)
- امنیت: JWT از `.env`، CORS محدود، توکن ۶۰ روزه
- Weekly Report Lock سمت سرور (جدول `weekly_reports`)
- Forgot/Reset Password با ایمیل
- Manual Backup واقعی (JSON در `backend/backups/`)
- AI Company Research واقعی (web_search grounded، نه حدسی)
- **[جدید]** تفکیک `companies.py` (۹۷۱ خط) به پکیج ماژولار `routers/companies/` (۱۳ فایل تخصصی) — بدون تغییر در API، تست‌شده با ۳۵ endpoint

### تمام‌شده — Cloud Deploy (2026-07-18)
- [x] Backend روی Railway، زنده روی `https://archon-production-b8a6.up.railway.app`
- [x] Postgres روی Railway، در همون پروژه‌ی backend
- [x] Frontend روی Vercel، زنده روی `https://archon-hazel.vercel.app`
- [x] `migrate_to_postgres.py` اجرا شد — ۷۲ ردیف منتقل شد، داده‌ی واقعی روی production تأیید شد
- [x] لاگین واقعی از production تست و تأیید شد
- [x] Add Company، Generate Email (AI)، Send Email (Resend) — همه تست و تأیید شدن
- [x] ایمیل از SMTP خام به Resend (HTTPS API) مهاجرت کرد — چون Railway پورت‌های SMTP رو مسدود می‌کنه

### تمام‌شده — R2 + Landing + Signup (2026-07-18/19)
- [x] **File Storage روی Cloudflare R2** — سرویس آپلود (boto3) + endpoint `POST /auth/upload` + سه هندلر فرانت (آواتار، پورتفولیو). عکس‌ها به‌جای base64 روی R2 می‌رن. **کد آماده، نیاز به ست‌کردن ۵ env var روی Railway** (R2_ACCOUNT_ID و...). تا اون موقع fallback به base64 کار می‌کنه.
- [x] **Landing Page عمومی** روی `/` (فایل static در `public/landing.html`، دوزبانه EN/FA). داشبورد رفت به `/dashboard`. کاربر لاگین‌شده خودکار به داشبورد هدایت می‌شه.
- [x] **Signup (Waitlist)** — چون دیتا تا Phase 5 مشترکه، signup عمومی یه waitlist امنه: `POST /auth/signup` (عمومی) + صفحه‌ی `/signup` + `GET /auth/waitlist` (فقط ادمین). CTAهای لندینگ به `/signup?plan=X` وصلن.

### تمام‌شده — دامنه، R2، و اصلاحات نهایی (2026-07-21)
- [x] ۵ env var مربوط به R2 روی Railway ست شد — آپلود واقعاً روی R2 می‌ره (تست‌شده)
- [x] `FRONTEND_URL` ست شد — لینک فورگت‌پسورد درست شد
- [x] **دامنه‌ی `app.armiladesign.com` وصل شد** (Vercel، DNS خودکار). سایت پورتفولیو روی `armiladesign.com` دست‌نخورده. `ALLOWED_ORIGINS` هم آپدیت شد.
- [x] تست‌های production: attachment، Weekly Report، Daily Tasks، پروفایل عمومی — همه تأیید شدن
- [x] دسترسی: Admin Panel/Users/Waitlist فقط ادمین؛ Admin Panel ریسپانسیو شد
- [x] پابلیک پروفایل: باگ ذخیره‌نشدن در سرور رفع شد (فقط localStorage بود)
- [x] پورتفولیو: ادیت پروژه (عنوان/توضیح/لینک) + alt text برای هر عکس
- [x] **[امنیتی]** endpointهای بک‌آپ اصلاً احراز هویت نداشتن — فقط ادمین شدن + endpoint دانلود امن اضافه شد

**کار عملیاتی باقی‌مانده از Phase 4 (نه کد، هنوز انجام نشده):** یک بک‌آپ رسمی از production بگیر و با دکمه‌ی **Download** یه نسخه‌ش رو جایی بیرون از Railway نگه دار.

---

## ۳. فازها — تاریخچه و پیش رو

### ✅ Phase 5 — SaaS Launch *(تکمیل شد)*

> ترتیب بر اساس **وابستگی** چیده شده — هر مرحله پیش‌نیاز مرحله‌ی بعدیه. از قدم ۱ شروع می‌کنیم.

**قدم ۱ — Multi-tenant واقعی** 🔴 *پایه‌ی همه چیز، بزرگ‌ترین کار*
- الان `companies` هیچ فیلد مالکیت نداره و status/heat/favorite/notes/campaigns/tasks/reports بین همه‌ی کاربرا مشترکه
- **مدل موردنظر:** catalog شرکت‌ها **مشترک** بمونه (هدف ۳۰۰۰ شرکت)، ولی **state هر کاربر جدا** باشه (یه جدول overlay با `user_id` برای status/favorite/heat + افزودن `user_id` به notes/campaigns/daily_tasks/weekly_reports)
- **نکته‌ی کلیدی:** ترتیب نمایش شرکت‌ها باید برای هر کاربر **متفاوت/رندوم** باشه (seed مخصوص هر اکانت) تا با ۳۰۰۰ شرکت، همه فقط سراغ ۲۰ تای اول نرن. یه گزینه‌ی sort «new» هم اضافه بشه
- ⚠️ migration یک‌طرفه روی Postgres زنده — قبلش حتماً بک‌آپ

**✅ قدم ۲ — Plan Limits Enforcement + نمایش اعتبار** *(انجام شد 2026-07-22)*
- جدول `plan_limits` قابل‌ویرایش از Admin Panel (سقف شرکت/ایمیل/دوره هر پلن). مقادیر hardcode نیستن؛ ادمین لحظه‌ای تغییرشون می‌ده.
- سقف پیش‌فرض: trial 10co/10em/7d · basic 50/30/30 · pro 500/300/30 · agency نامحدود (-1)
- Enforcement: ساخت شرکت، اولین engage با شرکت مشترک، و ارسال ایمیل — همه با ۴۰۳ واضح بلاک می‌شن سر سقف
- انقضا: `plan_expires_at` روی User؛ بعد از انقضا کاربر (غیرادمین) بلاک می‌شه تا تمدید
- ویجت اعتبار در داشبورد (شرکت/ایمیل باقی‌مانده + روزهای مونده)، برای پلن نامحدود مخفیه
- شمارش واقعی مصرف هر کاربر (ایمیل ارسالی، تعداد شرکت)
- نمایش باقی‌مانده تو داشبورد (مثلاً «۳۲ از ۵۰ ایمیل باقی‌مونده»)
- auto-disable بعد از ۳۰ روز یا اتمام سهمیه → نیاز به تمدید

**✅ قدم ۳ — پلن رایگان ۷ روزه** *(انجام شد 2026-07-23)*
- CTA دوزبانه «۷ روز رایگان امتحان کن» در لندینگ → `/signup?plan=trial`
- اعتبارسنجی پلن در signup (قبلاً هر رشته‌ای قبول می‌شد — می‌شد agency رو خودانتخاب کرد!)
- `/auth/me` اعداد سقف رو از جدول قابل‌ویرایش `plan_limits` می‌گیره + `plan_expires_at` برمی‌گردونه
- مکانیزم تمدید: ادمین پلن کاربر رو ست کنه (حتی همون پلن) → پنجره‌ی جدید و کاربر منقضی دوباره فعال
- badge پلن Trial (سبز) در همه‌جای UI
- ⏭ *باقی برای آینده:* نمایش فقط چند کشور محدود برای trial و قفل بقیه

**✅ قدم ۴ — باز کردن Signup واقعی** *(انجام شد 2026-07-23)*
- ثبت‌نام **trial فوریه**: اکانت بلافاصله ساخته می‌شه، توکن برمی‌گرده و کاربر مستقیم وارد داشبورد می‌شه (بدون انتظار برای approve)
- پلن‌های **پولی عمداً همچنان waitlist** می‌مونن تا Stripe بیاد — وگرنه انتخاب «pro» یعنی دادن پلن پولی به‌صورت رایگان
- ثبت‌نام‌های فوری هم به‌عنوان ردیف `approved` در waitlist ثبت می‌شن تا ادمین یک لیست واحد از همه داشته باشه
- کنترل با `SELF_SERVE_PLANS` — موقع اضافه‌شدن Stripe، پلن‌های پولی به همین مسیر فوری منتقل می‌شن

**✅ قدم ۵ — پرداخت دستی (جایگزین Stripe)** *(انجام شد 2026-07-23)*
- ⛔ **چرا Stripe نه:** Stripe ایران رو پشتیبانی نمی‌کنه، و درگاه‌های ایرانی (زرین‌پال/زیبال/آی‌دی‌پی) شخصیت حقوقی ایرانی + نماد اعتماد می‌خوان که با شرکت اسپانیایی Armila Design قابل دریافت نیست
- **راه‌حل:** لایه‌ی پرداخت دستی مستقل از درگاه — کاربر واریز می‌کنه و شماره پیگیری ثبت می‌کنه، ادمین تأیید می‌کنه، پلن با دوره‌ی جدید فعال می‌شه
- قیمت **دوارزی** (تومان + دلار) قابل ویرایش در Admin Panel
- دستورالعمل پرداخت دوزبانه (شماره کارت و…) قابل ویرایش در `/payments`
- صفحه‌ی `/upgrade` برای کاربر + `/payments` برای ادمین (تأیید/رد با دلیل) + badge نوتیفیکیشن
- 🔌 **مسیر آینده:** هر درگاهی (ایرانی یا بین‌المللی) به‌عنوان یک `method` جدید وصل می‌شه؛ بقیه‌ی سیستم دست‌نخورده می‌مونه

**✅ قدم ۶ — موارد مستقل** *(انجام شد 2026-07-24)*
- ✅ **AI Lead Discovery** — Claude با جستجوی وب شرکت‌های جدید پیشنهاد می‌ده، تکراری‌ها (بر اساس نام و دامنه) حذف می‌شن، و قبل از افزودن به کاتالوگ باید تأیید کنی. فقط ادمین.
- ✅ **User Email Analytics** — برای هر کاربر: تولیدشده / ارسالی / پاسخ / نرخ پاسخ + نمودار ۶ ماهه، در صفحه‌ی Analytics (کاملاً جدا برای هر اکانت)
- ✅ **Admin User Export** — خروجی CSV از همه‌ی کاربران با پلن، وضعیت و آمار فعالیت (مستقیم در Excel باز می‌شه)
- ✅ **دسترسی ادمین به دیتای هر ممبر** — دکمه‌ی View در صفحه‌ی Users: پلن، فعالیت، پروفایل، لینک عمومی و دکمه‌ی ایمیل مستقیم

**✅ قدم ۷ — قفل کشورها + بازبینی امنیتی + Lead Hunter** *(انجام شد 2026-07-31)*
- جزئیات کامل در بالای فایل (بندهای الف تا د).

### باقی‌مانده Phase 5 (نیاز به اقدام بیرون از کد)

- ⏸ **Gmail OAuth Connect** — نیاز به ساخت پروژه در Google Cloud + تأیید OAuth consent screen توسط Milad داره. تا اون موقع ارسال ایمیل از طریق Resend کار می‌کنه. *(کد نوشته نشده — منتظر تصمیم و اکانت گوگل)*
- 🔄 **Data Collection Campaign** — کار **تجاری/دستی** نه کدنویسی. روش عملیاتی‌اش در `leads/PLAYBOOK.md` مستند شد و با `/hunt` اجرا می‌شود.
  **وضعیت فعلی (2026-08-23): ۴٬۰۱۹ شرکت در ۴۷ کشور — هدف اول ۳۰۰۰ رد شد ✅**
  ۱۲ بازار اول: آمریکا ۵۱۰ · بریتانیا ۳۵۰ · امارات ۳۴۳ · عربستان ۲۵۷ · آلمان ۲۵۵ · استرالیا ۱۹۰ · کانادا ۱۷۳ · هلند ۱۷۰ · سوئیس ۱۵۶ · ایتالیا ۱۲۳ · روسیه ۱۰۷ · فرانسه ۱۰۳.
  هدف بعدی ۶۰۰۰+.

**از فاز ۵ نقشه راه، این دو مورد زودتر (در فاز ۴) انجام شدن:**
- ✅ Onboarding Flow — راهنمای تعاملی spotlight دوزبانه
- ✅ Landing Page با pricing و features

**مواردی که در نقشه راه اصلی نبودن و از بحث‌های بعدی اضافه شدن:** Multi-tenant واقعی (قدم ۱)، پلن رایگان ۷ روزه (قدم ۳)، باز کردن signup واقعی (قدم ۴).

### چک‌لیست تست production برای Phase 5 *(هنوز انجام نشده)*

**Phase 5 از نظر کدنویسی بسته‌ست**، ولی این موارد روی production تأیید نشدن:
1. یک اکانت trial بساز → باید فقط کشورهای مجاز رو ببینه، اسم شرکت‌ها دیده بشه ولی ایمیل/سایت بلور باشه، و دکمه‌ی Unlock کار کنه.
2. یک اکانت pro بساز و **تأیید نکن** → باید همه‌چیز (حتی اسم شرکت) بلور باشه و نتونه ایمیل بسازه.
3. `plan_expires_at` یک اکانت رو دستی به گذشته ببر → باید قفل بشه ولی **همچنان بتونه به `/upgrade` بره و پرداخت ثبت کنه**.
4. Admin Panel → کشورهای مجاز هر پلن رو چک/تنظیم کن (پیش‌فرض trial سه کشوره).
5. `/discovery` → یک Scout واقعی اجرا کن، چندتا رو تیک بردار، Research بزن و ببین امتیازها و citationها منطقی‌ان.
6. Admin Panel → **Recalculate Scores** رو بزن تا کل کاتالوگ با فرمول جدید امتیاز بگیره (امتیازهای فعلی با فرمول قدیمی حساب شدن).

⚠️ **نکته‌ی migration:** ستون‌های `plan_limits.allowed_countries` و `companies.score_breakdown`، و دو جدول `discovery_hunts` / `discovery_runs` موقع اولین استارت روی Railway خودکار ساخته می‌شن. قبل از deploy یک بک‌آپ بگیر (روال همیشگی).

⚠️ **تغییر رفتاری که باید بدونی:** فلگ‌های پلن حالا واقعاً enforce می‌شن. یعنی طبق مقادیر فعلی `PLAN_LIMITS` در `backend/app/routers/auth.py`، پلن‌های **trial و basic دیگه Market Map، Weekly Report و AI Search ندارن**. اگه این رو نمی‌خوای، اون سه فلگ رو در همون فایل `True` کن.

### ✅ Phase 6 — Project Marketplace *(تکمیل شد 2026-08-23)*

هر ۷ فاز نقشه راه مارکت‌پلیس پیاده و تست شد. جزئیات کامل در بخش «سشن 2026-08-23» بالای همین فایل.

| فاز | چی شد |
|---|---|
| ۰ | زیرساخت: ۸ مدل جدید، پکیج `routers/marketplace/`، فلگ دسترسی |
| ۱ | **Project Board** — پست پروژه، تابلوی باز، ارسال/پذیرش/رد/انصراف پروپوزال |
| ۲ | **Contract View** — لیست و جزئیات قرارداد با نمای متفاوت برای هر نقش |
| ۳ | **پرداخت دستی مایلستون** — fund → تأیید ادمین → deliver → approve → payout |
| ۴ | **چت** بین کارفرما و فریلنسر (polling، با read receipt) |
| ۵ | **پنل ادمین** — تأیید رسیدها، همه‌ی پروژه‌ها/قراردادها، ثبت payout |
| ۶ | **Review/Rating** — بعد از تکمیل قرارداد، نمایش در پروپوزال و پروفایل عمومی |
| ۷ | **محدودیت‌های بتا** — سقف مبلغ قرارداد + بنر یکپارچه‌ی Beta |

**دور دوم (بعد از تست واقعی Milad)** — جزئیات در بخش «دور دوم» بالای فایل:

| # | چی اضافه شد |
|---|---|
| ۱ | **آپدیت زنده** بدون رفرش (قرارداد ۶ثانیه، پروژه ۸ثانیه) |
| ۲ | **نوتیفیکیشن** — زنگوله + ایمیل برای مواردی که پول منتظر ادمینه |
| ۳ | **احراز هویت + اطلاعات بانکی** (`/verification`) با تأیید ادمین |
| ۴ | **جزئیات قرارداد و پرداخت** در پنل ادمین با اطلاعات بانکی فریلنسر |
| ۵ | **پیام مستقیم** بین هر دو کاربر (`Conversation`) |
| ۶ | **ریویو در پروفایل** — امتیاز، درصد رضایت، کامنت‌ها، دوطرفه |
| ۷ | **ویرایش/حذف پروژه** (حذف بعد از بسته‌شدن قرارداد ممنوع) |
| ۸ | **پروفایل داخلی** `/members/[id]` — نام هر کاربر همه‌جا لینک شده |
| ۹ | **دکمه Message** در جدول Users ادمین |

**⚠️ باقی‌مانده (عملیاتی، نه کد):**
1. **تست روی production** — یک اکانت کارفرما و یک فریلنسر بساز، کل چرخه رو طی کن (پروژه → پروپوزال → قرارداد → فاند → تأیید → تحویل → payout). لوکال با ۲۲۵ تست پوشش داده شده ولی production هنوز نه.
2. **اطلاعات پرداخت** باید در Admin Panel ست شده باشه، وگرنه پنل فاند بنر هشدار نشون می‌ده.
3. **سقف قرارداد** پیش‌فرض $۵۰۰ — در Marketplace Admin → تب Payouts قابل تغییره.
4. **`FRONTEND_URL` روی Railway** باید ست باشه وگرنه لینک داخل ایمیل نوتیفیکیشن‌های ادمین کار نمی‌کنه.

⚠️ **نکته‌ی migration:** جداول `mp_*` (شامل `mp_conversations`, `mp_notifications`, `mp_user_verification`) و ستون‌های جدید `users` (`marketplace_beta_enabled`, `account_mode`, `skills`, `hourly_rate`) موقع اولین استارت روی Railway خودکار ساخته می‌شن. دو backfill یک‌باره هم اجرا می‌شه: مارکت‌پلیس برای همه‌ی اکانت‌های موجود روشن می‌شه، و برای هر قرارداد موجود یک گفتگو ساخته و پیام‌هاش وصل می‌شه. **قبل از deploy بک‌آپ بگیر.**

### 🎯 Phase 7 — Ecosystem & Scale (فاز بعدی)
AI project matching، Market Intelligence Reports، Mobile App، Data API ($299/mo)، Verified Badge Program، Enterprise plans.

*(جزئیات کامل هر فاز در `Vision and Roadmap V04.html`)*

---

## ۴. ابزارها و زیرساخت جانبی
- **GitHub:** متصل، push/pull از طریق git کار می‌کند (بدون `gh` CLI)
- **VS Code:** دسترسی computer-use در سطح "click" (بدون تایپ) — برای دیدن/کلیک UI
- **GitHub Desktop:** دسترسی کامل computer-use
- **Graphify:** knowledge graph کدبیس نصب و ساخته شده (`archon/graphify-out/`) — استفاده‌ی موردی برای سوالات ساختاری/چندفایلی، نه هر درخواست (برای صرفه‌جویی توکن)

---

## ۵. باگ‌های اخیر رفع‌شده

**در طول Deploy واقعی پیدا و رفع شد (2026-07-18):**
- `crawl4ai==0.2.77` تو `requirements.txt` دیگه روی PyPI موجود نبود و build Railway رو fail می‌کرد — حذف شد (هیچ‌جای کد استفاده نمی‌شد)
- `python-multipart` (لازم برای آپلود CSV) در `requirements.txt` نبود — اضافه شد
- `.python-version` اضافه شد تا Railway نسخه‌ی سازگار با `psycopg2-binary` انتخاب کنه
- Vercel build fail می‌شد: تضاد peer-dependency بین `react-simple-maps` و React 19 — با `.npmrc` (`legacy-peer-deps=true`) حل شد
- Vercel build fail می‌شد: خطای TypeScript (`JSX.Element` namespace در Sidebar، typeهای گم‌شده در map/page.tsx، `useSearchParams` بدون Suspense در reset-password) — هر سه رفع و با `npm run build` لوکال تأیید شد
- **[بحرانی]** آدرس بک‌اند (`http://localhost:8000`) در ۱۷ فایل فرانت‌اند هاردکد بود — بدون این fix، دیپلوی روی Vercel کاملاً از کار می‌افتاد. الان همه از `NEXT_PUBLIC_API_URL` می‌خونن.
- **[بحرانی]** بعد از migration، sequence شماره‌گذاری خودکار Postgres برای هر ۸ جدول درست نشده بود (یه خط SQL خام بدون `text()` در SQLAlchemy 2.x silent-fail می‌کرد) — هر INSERT جدید (Add Company، Generate Email، Tasks، Weekly Report) با خطای duplicate-key crash می‌کرد و به‌اشتباه به شکل خطای CORS در مرورگر دیده می‌شد. رفع شد + sequenceهای production دستی ریست شدن.
- **[بحرانی]** ارسال ایمیل با SMTP خام کار نمی‌کرد — اول `Network is unreachable` (نبود مسیر IPv6 روی Railway)، بعد از فیکس IPv4 هم `Connection timed out` (Railway پورت‌های SMTP رو مسدود می‌کنه). رفع نهایی: مهاجرت کامل به **Resend** (HTTPS API) برای send-email و forgot-password — نیاز به `RESEND_API_KEY` و `RESEND_FROM_EMAIL` روی دامنه‌ی verified.
- `migrate_to_postgres.py` جدول `weekly_reports` رو migrate نمی‌کرد (قفل ۷روزه‌ی گزارش هفتگی گم می‌شد بعد از مهاجرت) — رفع شد.
- `DEPLOY_CHECKLIST.md` ساخته شد — چک‌لیست کامل قبل از Deploy (env vars، migration، DNS، smoke tests، rollback) و کاملاً بازبینی/تست شد
- Backup path هاردکد بود و روی Railway (filesystem ephemeral) با هر redeploy پاک می‌شد — الان از `BACKUP_DIR` env var پشتیبانی می‌کنه؛ نیاز به ساخت Railway Volume + تنظیم env var قبل از Deploy واقعی (جزئیات در DEPLOY_CHECKLIST.md بخش ۶)
- Sidebar جابجایی هنگام اسکرول — چند تلاش اولیه (minHeight→height، z-index) فقط علائم رو کم کردن؛ علت ریشه‌ای (transform روی `.page-enter` که containing block فیکس‌ها رو می‌شکست) در 2026-07-31 پیدا و رفع شد، جزئیات در بالای فایل
- دکمه‌ی «Email this user» که با mailto: بدون کلاینت پیش‌فرض هیچ کاری نمی‌کرد — کپی به کلیپ‌بورد به‌عنوان fallback اضافه شد (2026-07-31)
- **[بحرانی/امنیتی]** `export/csv` کل کاتالوگ رو به هر اکانت لاگین‌شده می‌داد — trial رایگان = دانلود کل دیتابیس (2026-07-31)
- **[بحرانی]** کاربر منقضی به‌خاطر ۴۰۳ در `get_current_user` به صفحه‌ی پرداخت هم دسترسی نداشت — یعنی نمی‌تونست تمدید کنه (2026-07-31)
- **[امنیتی]** هر ممبر می‌تونست فیلدهای کاتالوگ مشترک رو برای همه ویرایش کنه (2026-07-31)
- **[امنیتی]** برگردوندن ایمیل `sent` به `draft` سهمیه‌ی ارسال رو ریست می‌کرد (2026-07-31)
- **[امنیتی]** فلگ‌های فیچر پلن‌ها تبلیغ می‌شدن ولی هیچ‌جا چک نمی‌شدن (2026-07-31)
- Score circle strokeDasharray بدون ضریب مقیاس
- حذف Note امکان‌پذیر نبود (DELETE endpoint اضافه شد)
- `class_mapper` import اشتباه در تفکیک companies.py (از `sqlalchemy.orm` نه `sqlalchemy`)

**سشن مارکت‌پلیس (2026-08-23):**
- **[بحرانی]** حذف کاربر روی Postgres با FK violation کرش می‌کرد وقتی اکانت تاریخچه‌ی مارکت‌پلیس داشت — SQLite این رو چک نمی‌کنه پس لوکال بی‌صدا رد می‌شد. الان بلاک می‌شه با پیام «به‌جاش Disable کن».
- **[بحرانی]** `marketplace_beta_enabled` پیش‌فرض `False` بود و فقط ادمین از گیت رد می‌شد → اکانت Pro اصلاً بخش Projects رو نمی‌دید. فیچر عملاً فقط ادمین بود.
- **[یکپارچگی]** مایلستون‌ها لازم نبود با مبلغ قرارداد جمع بزنن → سقف قرارداد قابل دور زدن بود و اجزای یک قرارداد می‌تونستن با جمع کلش اختلاف داشته باشن.
- **[یکپارچگی]** فرم فاند هر مبلغی رو قبول می‌کرد → یک پرداخت کسری که اشتباهی تأیید بشه، فریلنسر رو طلبکار می‌ذاشت در حالی که مایلستون «funded» علامت خورده.
- **[UI]** پنل چت در صفحه Messages فقط نصف صفحه رو پر می‌کرد (`ContractChat` سقف ثابت ۴۸۰px داشت که برای صفحه‌ی قرارداد درست بود ولی برای Messages نه).
- **[UI]** صفحه Messages روی موبایل مستقیم وارد گفتگو می‌شد به‌جای نمایش لیست — `useIsMobile` در اولین رندر هنوز مقدار نگرفته بود.
- **[UI]** پنل فاند شماره کارت و پی‌پال نشون نمی‌داد — اطلاعات پرداخت فقط در یک endpoint **فقط-ادمین** بود.
- **[داده]** کاتالوگ هم `USA`/`United States` و هم `UAE`/`United Arab Emirates` داشت — یک بازار در همه‌جا که بر اساس کشور گروه‌بندی می‌کنه دو تیکه می‌شد.

**دور دوم مارکت‌پلیس (2026-08-23):**
- **[UX]** هر پیام یا تغییر قرارداد فقط با رفرش دیده می‌شد → polling با توقف هنگام باز بودن فرم.
- **[UX]** ادمین در مرحله‌ی آخر هیچ اطلاعاتی از فریلنسر برای واریز نداشت → صفحه‌ی جزئیات با اطلاعات بانکی و دکمه پرداخت.
- **[UX]** هیچ اعلانی وجود نداشت؛ ادمین باید دستی چک می‌کرد که پروژه‌ای آماده‌ی واریزه → سیستم نوتیفیکیشن + ایمیل.
- **[UX]** ریویوها فقط در صفحه‌ی قرارداد بودن و در پروفایل ثبت نمی‌شدن → بخش Reputation دوطرفه.
- **[UI]** پنل چت در صفحه Messages نصف صفحه بود (سقف ۴۸۰px که برای صفحه‌ی قرارداد درست بود) → حالت `fill`.
- **[UI]** صفحه Messages روی موبایل مستقیم وارد گفتگو می‌شد (`useIsMobile` در اولین رندر مقدار نداشت).
- **[یکپارچگی]** پروپوزال‌های پروژه‌ی حذف‌شده بی‌صدا یتیم می‌موندن → حذف پروژه بعد از قرارداد ممنوع، و بقیه نوتیف می‌گیرن.

---

## ۶. تصمیمات و محدودیت‌های شناخته‌شده
- Plan Limits (محدودیت تعداد شرکت بر پلن) عمداً به Phase 5 موکول شد — چون `companies` table هنوز مشترک بین کاربراست، نه per-user
- `backend/app/crawler/` در dependencies هست (`crawl4ai`) ولی خالیه — زیرساخت جمع‌آوری داده هنوز ساخته نشده
- نصب `graphifyy` (دو-Y) به‌جای `graphify` — این عمداً درسته طبق README رسمی پروژه (typosquat هشدار دقیقاً برعکس بود)

**تصمیمات مارکت‌پلیس (2026-08-23):**
- ⛔ **escrow واقعی نمی‌سازیم.** نگه‌داشتن پول شخص ثالث یعنی تبدیل شدن به money transmitter/custodian که مجوز EMI می‌خواد؛ با تحریم‌ها Stripe Connect هم در دسترس نیست. به‌جاش الگوی «پرداخت دستی + تأیید ادمین» که از قبل برای پلن‌ها داشتیم بسط داده شد. **مسیر آینده:** وقتی درگاه ایرانی آماده شد، فقط مرحله‌ی تأیید واریز خودکار می‌شه.
- ⛔ **اکانت جدا برای کارفرما/فریلنسر نساختیم.** یک اکانت با `account_mode` قابل سوییچ — چون اکانت جدا یعنی اعتبار و ریویوهای یک نفر بین دو هویت تقسیم بشه. اجازه‌ی واقعی از `client_id`/`freelancer_id` روی هر قرارداد میاد، نه از mode.
- **چت با polling نه websocket** — ساده‌تر و قابل‌اتکاتر؛ ۵ ثانیه تأخیر برای یک thread مذاکره کافیه.
- **مارکت‌پلیس برای همه بازه، نه opt-in** — بخشی که کسی نمی‌بینه تست هم نمی‌شه. فلگ به‌عنوان کلید خاموش‌کردن per-account موند.
- **سقف قرارداد لحظه‌ی accept چک می‌شه** نه موقع پست پروژه — چون اونجاست که پول واقعاً متعهد می‌شه؛ بودجه‌ی پروژه فقط تخمینه.
- **نقشه: فقط مرزبندی، بدون مارکر.** یک نسخه با نقطه برای کشورهای کوچیک تست شد ولی رد شد (ظاهر حرفه‌ای نقشه رو خراب می‌کرد). به‌جاش داده‌ی ۱:۱۰m + زوم خودکار.
- **دو صفحه‌ی پروفایل، عمداً.** `/u/[username]` صفحه‌ای که کاربر انتخاب می‌کنه روی وب عمومی باشه (به `is_public` احترام می‌ذاره)؛ `/members/[id]` نمای داخل مارکت‌پلیس که برای هر کاربر لاگین‌شده کار می‌کنه. **دلیل:** هویت کسی که باهاش رقابت یا معامله می‌کنی نمی‌تونه اختیاری باشه — ولی نمای داخلی هم فقط چیزهایی رو نشون می‌ده که از قبل قابل دیدن بودن، نه اطلاعات هویتی/بانکی.
- **اطلاعات KYC/بانکی در جدول جداگانه** (`mp_user_verification`)، نه روی `users`. نیمه‌ی حساسه و فقط خود شخص و ادمین باید ببینن.
- **حذف پروژه بعد از بسته‌شدن قرارداد ممنوع** — فریلنسر روش حق داره. حذف کاربری که تاریخچه‌ی قرارداد داره هم ممنوع (به‌جاش Disable).
- **ساختن نوتیفیکیشن هرگز عملیات اصلی رو fail نمی‌کنه** — payout انجام‌شده‌ای که اعلامش خطا داده، بازهم payout انجام‌شده‌ست.
- **آپدیت زنده با polling** (نه websocket)، و **وقتی فرمی باز باشه متوقف می‌شه** تا ورودی نیمه‌تمام کاربر پاک نشه.
- **کره‌ی سه‌بعدی مثل Google Earth:** فنی شدنیه (`react-globe.gl`)، ولی رد شد — WebGL سنگین‌تر، موبایل ضعیف‌تر، و برای یک ابزار داخلی که نقشه فیچر جانبیشه ارزش نداره. تصاویر ماهواره‌ای واقعی اصلاً شدنی نیست (پتابایت داده‌ی لایسنس‌شده).

---

## ۷. قدم بعدی پیشنهادی
_(این بخش رو با هر آپدیت تغییر بده)_

**Phase 6 بسته شد.** سه مسیر پیش رو — به ترتیب اولویت پیشنهادی:

### ۱. تست production مارکت‌پلیس 🔴 *فوری‌ترین*
همه‌چیز لوکال با ۲۲۵ تست تأیید شده ولی روی production هنوز نه. قبلش:
- **بک‌آپ بگیر** (migrationها یک‌طرفه‌ان — به‌ویژه بازسازی جدول پیام‌ها)
- اطلاعات پرداخت رو در Admin Panel ست کن (وگرنه پنل فاند بنر هشدار می‌ده)
- `FRONTEND_URL` روی Railway ست باشه (برای لینک داخل ایمیل نوتیفیکیشن‌ها)
- دو اکانت تست بساز (یکی کارفرما، یکی فریلنسر)، هر دو **احراز هویت** رو تکمیل کنن، و کل چرخه رو طی کن
- سقف قرارداد رو در Marketplace Admin چک کن (پیش‌فرض $۵۰۰)
- چک کن ایمیل نوتیفیکیشن ادمین واقعاً می‌رسه (Resend)

### ۲. ادامه‌ی جمع‌آوری لید 🟡 *کار تجاری، نه کد*
۴٬۰۱۹ شرکت داریم، هدف بعدی ۶۰۰۰+. با `/hunt` و طبق `leads/PLAYBOOK.md`.

### ۳. Phase 7 — Ecosystem & Scale 🟢 *وقتی دو مورد بالا جا افتاد*
AI project matching، Market Intelligence Reports، Data API، Verified Badge، Enterprise plans.

### موارد باز از فازهای قبل
- ⏸ **Gmail OAuth** — نیاز به پروژه در Google Cloud + تأیید consent screen (تصمیم با Milad). فعلاً Resend کار می‌کنه.
- ⏸ **چک‌لیست تست production فاز ۵** (بخش ۳ همین فایل) — هنوز انجام نشده.
- ⏸ **بک‌آپ رسمی از production** و نگه‌داشتن یک نسخه بیرون از Railway.
3. ترتیب نمایش با seed مخصوص هر کاربر + گزینه‌ی sort «new» رو از همون اول در query اصلی لحاظ کن (بعداً اضافه‌کردنش سخت‌تره)
